# CLAUDE.md — הנחיות פרויקט BOT

> נטען אוטומטית בתחילת כל סשן של
> `Claude Code`
> בתיקייה זו. חל על כל תת-התיקיות.
> מיקום: `C:\Users\User\Documents\landbot-runner-clean\CLAUDE.md`
> הרץ `/memory` כדי לוודא שהקובץ נטען.
>
> **אומת ב-31 ביולי 2026, סשן ערב.**

---

## מסמכי המקור

קובץ זה מכיל **הנחיות עבודה בלבד**. הארכיטקטורה חיה במקום אחר:

`PROJECT_GUIDE.md`

בשורש — מבנה המערכת המלא והמאומת. **זהו מקור האמת.**

`docs\ENCRYPTION.md`

— שכבת ההצפנה והזהויות.

`docs\SESSION_HANDOFF.md`

— מצב עבודה נוכחי, מה נשבר ומה באוויר.

`docs\SESSION_LOG.md`

— יומן גולמי כרונולוגי, מוסף בלבד. **אינו תחת מעקב**

`Git`

**בכל שאלה על מבנה — קרא את המדריך. אל תסתמך על קובץ זה ואל תסתמך על זיכרון.**

**אזהרה שהתממשה:** גם המדריך עלול להיות מיושן. סעיף שקבע קיום מדיניות זמנית פתוחה הוביל לאבחון שגוי, כי המדיניות כבר לא הייתה קיימת. **תיעוד הוא נקודת פתיחה, לא ראיה.**

---

## שפה ותקשורת

- ענה **בעברית בלבד**.
- מילה באנגלית באמצע משפט עברי — בשורה נפרדת, שורה ריקה לפניה ואחריה.
- אין לשבץ אותיות לטיניות בתוך מילים עבריות.
- טקסט מיושר לימין. היה תמציתי.

---

## עקרונות דיוק — חובה

1. **דיוק מעל הכול.** רק מידע מאומת. אין לנחש ואין להשלים פערים.
2. **כלל האימות.** כל מה שניתן לאמת — אמת בפועל לפני התשובה. מבנה טבלאות ושמות עמודות **אינם** ניתנים לאימות מעץ הקבצים — נדרשת שאילתה ישירה.
3. **קדימות למידע שסופק.** קובץ או צילום שהמשתמש נתן גובר. בסתירה — לציין במפורש.
4. **כלל העצירה.** בחוסר ודאות: "לא ניתן לאשר בוודאות. נדרש אימות נוסף."
5. **טיפול בטעויות.** להודות מיד, להסביר את מקור הטעות, לספק תשובה מתוקנת.
6. **התאמה לישראל** בכל נושא משפטי, רגולטורי, רפואי או בטיחותי.

### 7. אין להסיק התנהגות מסיכום — כלל חדש

שלוש טעויות בסשן 30.7 נבעו מהסתמכות על **סיכום** שהופק על ידי כלי, במקום קריאת הקוד הגולמי.

**כשמדובר בהתנהגות שממנה נגזרת פעולה — לקרוא את המקור, לא דוח על המקור.** בקש הדפסה מילולית עם מספרי שורות, ואל תסתפק בתיאור.

---

## כלל הטבלאות

**טבלה שקיימת לה מקבילה בסיומת** `_v2` **— ישנה ואינה בשימוש.**

**טבלה שאין לה מקבילה — פעילה, גם אם אין לה סיומת.** למשל:

`risk_phrases`

`app_config`

**חריג מאומת:**

`talk_read_flags`

**ללא סיומת היא הפעילה.** גרסת

`_v2`

היא הנטושה. ה־

`Viewer`

קורא את הראשונה בשורה 287, ושם גם חיות מדיניות הסינון.

> **הרגרסיה נסגרה — 31.7.2026.** השם הוחזר, וה־
>
> `Viewer`
>
> אומת חי מקצה לקצה. שלוש מדיניות הסינון שרדו את שינוי השם ולא אבד מידע.
>
> הסתירה בין סעיף 4.2 לסעיף 15.4 במדריך **הוכרעה לטובת 15.4** על שלוש ראיות בלתי
> תלויות: כתיבה אחרונה, מדיניות מסוננת, וקוד.
>
> **אין קובץ במאגר שמפנה ל־**
>
> `talk_read_flags_v2`
>
> **בשליפה, בכתיבה או בסינון — כלל.** היא מועמדת לשינוי שם. הנקודה העיוורת היחידה
> שנותרה היא
>
> `Landbot`

**הכלל אינו חל על משימות תזמון ועל תהליכי**

`GitHub Actions`

שם רצות במקביל גרסאות ישנות וחדשות, ושתיהן פעילות.

רשימת הטבלאות הפעילות המאומתות — בסעיף 4.2 של המדריך.

---

## שלוש תת-מערכות — אל תבלבל ביניהן

| # | תיאור | מצב |
|---|---|---|
| א | צינור הנתונים: גיליון ← מסד ← שיחות ← סיכום ← סיכון ← צפייה | חי ופעיל |
| ב | מסלול התשובה בזמן אמת: `runtime-corrected-response` | חי, בבוט הטיפולי |
| ג | סביבת הכוונון: `bot simulation` | בדיקות בלבד |

התיקייה

`bot simulation`

**מנותקת מייצור.** אינה נוגעת בנתוני מטופלים.

**אין להתייחס אליה כ"תיקיית העבודה הפעילה".** קבוצה א' היא המערכת המרכזית.

---

## שני הבוטים

הבוט הטיפולי →

`runtime-corrected-response`

→ פרומפט

`therapist`

מדריך הקורס → קריאה ישירה ל־

`OpenAI`

→ פרומפט

`course_guide`

---

## גיליונות — שלושה, נפרדים

`BOT_PATIENTS`

— כותרות:

`phone`

,

`user_text`

,

`therapy_track`

,

`name`

,

`active`

,

`status`

,

`psychologist`

,

`update`

`BOT_PROMPT`

— כותרות:

`prompt_key`

,

`description`

,

`user_text`

,

`update`

**אין בו טלפון**, ולכן שורות פרומפט אינן יכולות ליצור זהות מטופל.

`BOT_PSYCHLOGISTS`

— כותרות:

`phone`

,

`name`

,

`email`

,

`organization`

,

`text`

,

`active`

,

`therapy_track`

,

`update`

הטלפון נשמר שם **ללא אפס מוביל**; הפונקציה מוסיפה אותו.

---

## פרומפטים

הטבלה

`prompt_information_v2`

**קיימת ופעילה.** שש רשומות לפי

`prompt_key`

עמודת התוכן:

`user_text`

**חסר בטבלה:** אין עמודות גרסה או סטטוס פעיל. ניהול גרסאות לא מומש.

---

## Supabase

**Project Ref:**

`qcwimczsiuxkarwfiyai`

**פריסה:**

```
supabase functions deploy <name> --project-ref qcwimczsiuxkarwfiyai --no-verify-jwt
```

הדגל נדרש משום שהאימות בכותרת

`x-landbot-secret`

12 פונקציות

`Edge`

פרוסות. הרשימה בסעיף 6 של המדריך.

**משתני סביבה:**

`OPENAI_API_KEY`

`LANDBOT_WEBHOOK_SECRET`

`THERAPIST_MODEL`

`CORRECTOR_MODEL`

ברירת מחדל:

`gpt-5.4`

---

## סכנה פעילה — עומסי יתר בפונקציות

**מנוע המסד בוחר לפי טיפוסים, ולכן קריאה עלולה להגיע לגרסה ישנה בלי שתתקבל שגיאה.**

`supa_daily_migration`

— **חדש, התגלה ב-31.7.** שתי חתימות:

`(p_phone text)`

וללא ארגומנטים. חמישה עוטפים קוראים לה. חלק משרשרת

`V1`

הרדומה.

**תוקנו:**

`start_conversation_v2`

(27.7),

`upsert_users_information_v2_from_sheet`

(30.7) ו־

`insert_conversation_v2`

(31.7). בשלושתן נמחקה החתימה המיותרת לפי חתימה מלאה.

`upsert_psychologist_from_sheet`

(8.8) — **שתי** חתימות נמחקו, ונוצרה אחת בת שבעה פרמטרים. אומת במסד: חתימה אחת,

`oid 320400`.

### כלל הכרעה — מאומת 31.7

`PostgREST`

בוחר חתימה לפי **שמות המפתחות** בגוף

`JSON`

קריאה מתוך המסד בוחרת לפי **טיפוסים**.

לכן אותה פונקציה עשויה לשרת שני קוראים שונים בשתי חתימות שונות, ואף אחת מהן אינה מיותרת. ב־

`insert_conversation_v2`

הבוט הגיע לחתימת

`uuid`

לפי שמות, וטריגר במסד הגיע לחתימת

`text`

לפי מיקום. **יש לזהות איזה קורא משתמש באיזו חתימה לפני שקובעים שחתימה כלשהי מיותרת.**

**לפני כל שינוי בפונקציה — לבדוק אם קיימת לה חתימה נוספת.**

**לקח:** מחיקת פונקציה עם כמה חתימות דורשת ציון החתימה המדויקת, אחרת נכשלת בשקט ונראית כאילו החתימה "חזרה".

---

## אבטחה — שני כללים שנלמדו בדם

### פונקציה SECURITY DEFINER חייבת לסנן בגופה

`rpc_get_users_tzvira_v2_viewer`

נבנתה כדי **להגן**, והייתה החשיפה הגדולה ביותר שנמצאה: מפענחת, ללא סינון, ונגישה ל־

`anon`

היא עקפה את שער האימות ואת כל שמונה מדיניות ה־

`RLS`

**אין להישען על הרשאות, ואין להניח שמי שקורא לפונקציה כבר סוננה.**

### הרשאת EXECUTE אינה מעידה על כוונה

כמעט כל פונקציה בסכמה

`public`

פתוחה ל־

`anon`

, משום שזו **ברירת המחדל של**

`PostgreSQL`

**אין להסיק שפונקציה "נועדה" לשימוש חיצוני, ואין להסיק שמישהו פתח אותה.**

---

## סדר עדיפויות נוכחי — עודכן 31.7.2026 (ערב)

**קודם לכול — רגרסיה:** להחזיר את שם

`talk_read_flags`

ולאמת את ה־

`Viewer`

**הושלם ב-31.7:** סגירת עומס

`insert_conversation_v2`

(נבדק חי), הסרת הטריגר והפונקציה

`mirror_conversation_to_v2`

, השבתת משימת תזמון 38 ששרשרת

`V1`

רצה דרכה כל 15 דקות, סקירת שלוש הפונקציות הנגישות ל־

`anon`

, ושינוי שם לשבע טבלאות ישנות לסיומת

`_old_to_delete`

**נותר בעדיפות:**

1. **`decrypt_user_text`** — עדיין `SECURITY DEFINER` ופתוחה ל־`anon`. שבורה כרגע, אך שבור אינו סגור. הדפוס שעבד על `get_all_phones_and_talks` חל ישירות: גיבוי גוף, שינוי שם, ואז `revoke`.
2. **סיסמת המסד** נשמרה בטקסט גלוי ב־`.claude\settings.local.json`, שורות 16 ו-18. אומת שלא הגיעה ל־`GitHub`, אך יש להחליף אותה. פריט 26 ב־`SESSION_HANDOFF.md`.
3. **שלוש מדיניות `anon` פתוחות** נסעו יחד עם `users_information_old_to_delete`.
4. **מרווח התזמון של שרשרת `V2` אינו ידוע.** הוא קובע את ההשהיה בין סוף שיחה להופעת סיכון בפני מטפל. פריט 24.
5. **להשלים את מחיקת העמודות הגלויות** מ־`patient_identity_map`. שני חוסמים: `get_last_users_thread_v2` ו־`conversation_events_v2_view`.
6. **לבדוק אם `.range()` חל על `rpc`** — קריטי לפני מטופלים אמיתיים.
7. BOT MONITOR: שם פסיכולוג, וזמן סיום ב־`Conversation Ended`.

**נסגר ב-31.7 ערב — `get_all_phones_and_talks`.** גוף הפונקציה גובה, שמה שונה, ואז בוטלה הרשאת ההרצה מ־`anon`, `authenticated` ו־`public`. ה־`ACL` כולל כעת `postgres` ו־`service_role` בלבד. **שינוי השם לבדו לא הספיק** — ההרשאות נסעו לשם החדש.

**נסגר ב-31.7 ערב — `supa_guarded_run_log_details`.** 485 שורות עם טלפון ושם מלאים, שם שונה לסיומת `_old_to_delete`. הצרכן היחיד היה `supa_daily_migration_guarded`, שמופעל ממשימה 38 המושבתת.

**נסגר ב-31.7 ערב — נתיב העקיפה בשורה 111 של `supa-sync`.** אומת רדום בפועל: שתי קריאות חיות החזירו 200, כלומר הקוראים מעבירים `table` מפורש ואינם נופלים לברירת המחדל.

**מבוטל:** הפריט "להחזיר אימות במייל ולסגור את הכניסה העוקפת". אימות המייל **כבר מיושם**, והמילה

`bypass`

אינה קיימת באף קובץ

`HTML`

במאגר.

רשימת המשימות המלאה — בסעיף 16 של המדריך ובפרק

`OPEN`

של

`SESSION_HANDOFF.md`

---

## דרישות פיתוח

### כללים שנוספו ב-31.7.2026 — מטעויות שאירעו בפועל

- **בלוק קוד מכיל רק את הפקודה של הצעד הנוכחי.** צעדים עתידיים מתוארים במילים בלבד, ללא תחביר ניתן להרצה. פקודה הרסנית אינה נכתבת לפני שהאימות שקדם לה חזר. **הפרת הכלל הזו עלתה בגוף פונקציה שנמחק ללא גיבוי.**

- **גיבוי ברגע הסימון, לא ברגע המחיקה.** קריאת

`pg_get_functiondef()`

  מתבצעת כשאובייקט מסומן כמועמד למחיקה, לא בצעד שצמוד למחיקה.

- **חיוּת לפני ניתוח.** לפני שמנתחים הרשאות, מדיניות או גופי פונקציות — לבדוק אם הטבלה או הנתיב חיים בכלל. ספירה, כתיבה אחרונה, ומי הכותבים. ניתוח סיכון מלא לאובייקט מת הוא בזבוז.

- **נפח שורות אינו מדד לחיוּת.** טבלאות ישנות מלאות לרוב יותר מהחדשות בגלל נתוני בדיקה שנצברו. גבוה אינו פעיל, נמוך אינו נטוש. חיוּת נקבעת ממי כותב.

- **סקירת מסד אינה סקירת מערכת — ולסגור את הפער, לא רק להצהיר עליו.**

`pg_proc`

  ,

`information_schema.views`

  ו־

`cron.job`

  אינם רואים

`Landbot`

  , פונקציות

`Edge`

  , או את ה־

`Viewer`

  **לפני שינוי שם או מחיקה של טבלה — לחפש אותה בקבצי התיעוד.**

- **שינוי שם עדיף על מחיקה.** סיומת

`_old_to_delete`

  הופכת כל צעד להפיך בפקודה אחת, וגורמת לצרכן שנשכח להיכשל בקול.

- **שינוי שם אינו מסיר הרשאות ומדיניות.** הן נוסעות עם הטבלה. טבלה ששמה שונה ויש עליה מדיניות

`anon`

  פתוחה — עדיין חשופה.

- **שבור אינו סגור.** פונקציה שהטבלה שלה שונתה עדיין מחזיקה את ההרשאות שלה.

- **כל שאילתת קטלוג מסננת סכמה מפורשות.** צירוף

`pg_class`

  לפי שם בלבד עלול להצביע על אובייקט בסכמה אחרת.

- **תיחום מילה שלמה חובה בסריקות.** הביטוי

`~ '\mname\M'`

  — בלעדיו

`talk_read_flags`

  תופס גם את

`talk_read_flags_v2`

  וכל התוצאה שגויה.

- **תיעוד כל טעות.** כל טעות של

`Claude`

  נרשמת מיד כפריט מעקב בפרק

`MISTAKES`

  ב־

`SESSION_HANDOFF.md`

  , ובסיכום הסשן נכתב כאן הכלל שימנע את חזרתה.


- **לחפש קורא קיים לפני שמציעים בנייה.** לפני הצעה על פונקציה, עמודה, טבלה או תהליך חדשים — לאתר את המנגנון הקיים בשלושה מקומות: תיעוד (

`PROJECT_GUIDE.md`

  ו־

`SESSION_HANDOFF.md`

  ),

`pg_proc`

  , ו־

`information_schema.views`

  **"אפס קוראים במאגר" אינו נטישה** — נקודות הכניסה של

`Landbot`

  חיות בקנבס וללא עקבה בקוד. סעיף 22.19 במדריך.

- **תוספת לפני מחיקה.** כל שינוי מבני הוא תוספת. מחיקה היא שלב נפרד.

  **אך זהו אינו רישיון לדלג על המחיקה.** ההצפנה נוספה ושלב ההחלפה לא בוצע — התוצאה הייתה מערכת שנראית מוצפנת ופועלת גלוי. אותו דפוס חי כעת בעמודות הצל של

`risk_reviews_v2`

- **שאילתת בדיקה לפני מחיקה.** תמיד.

- **השבתה לפני מחיקה** לתהליכים.

- **מחיקת עמודה שוברת צרכנים מחוץ ל־**

`Viewer`

  לאמת את **כל** הצרכנים:

`scripts\`

,

`tools\`

, תהליכים, **וגם**

`pg_proc`

**וגם**

`information_schema.views`

  **סקירת מאגר אינה סקירת מערכת.** הסקירה שקבעה "צרכן אחד בלבד" הייתה נכונה ופספסה שני צרכנים במסד — פונקציה ו־

`view`

- **הסרת כותב אינה זהה למחיקתו.** לפני הסרת כתיבה — לוודא שקיים מסלול חלופי שכותב **בפועל**, ולא רק שהעמודה קיימת.

- אין עותקים ממוספרים של קבצים. הסיומות

`_v2`

`_safe`

`_final`

  אסורות על קבצים. מותרות על טבלאות.

- **נתוני בדיקה בערכים מזויפים בלבד.** מזהים אמיתיים עלולים לדרוס שורות חיות. **ושים לב:** שיחה שנרשמה ביומן הביקורת אינה ניתנת למחיקה כלל, וגם לא הזהות שלה.

- עבוד תמיד דרך

`Git`

. נתיבים מלאים. אין קוד פרוס שאינו ב־

`main`

- **מחזור**

`Git`

  **מלא תמיד:** הוספה, שמירה, דחיפה, אימות. השמטת הדחיפה היא שגיאה חוזרת — **ובדיקה בדפדפן לפני דחיפה בודקת את הקובץ הישן, כלומר אינה בודקת דבר.**

---

## כללים שנוספו ב-31.7.2026 — סשן ערב

- **שינוי שם מסתיר.**

`revoke`

  **סוגר.** הרשאות נוסעות עם השם החדש, ולכן חשיפה חוזרת ברגע שמישהו מחזיר את השם. בכל אובייקט נגיש ל־

`anon`

  הרצף הוא שלושה שלבים ולא שניים: גיבוי גוף, שינוי שם, ואז ביטול ההרשאה.

- **לפני הצעת מדידה — לנסח מה תוכיח תוצאה שלילית.** אם התשובה היא "כלום", זו אינה בדיקה. היעדר קריאות ביומן אינו הוכחה למוות; הוא מחייב בקרה שמאשרת שהמערכת בכלל פעלה באותו חלון. וגם אז — פונקציה מוגנת ב־

`JWT`

  שאיש לא הפעיל נראית זהה למתה.

- **סריקה שמחזירה אפס בלי בקרה אינה ממצא.** בכל סריקת צרכנים יש להריץ במקביל את אותה שיטה בדיוק על מזהה שידוע כחי. אם הבקרה חוזרת ריקה — השיטה שבורה ואין להסיק דבר. תקף גם לסריקת מסד וגם לסריקת קבצים. **זה קרה בפועל:** סריקת בקרה על

`talk_read_flags`

  החזירה אפס, למרות שהטבלה חיה.

- **מצב ההפעלה של תהליך אינו נמצא בקובץ.**

`GitHub Actions`

  שומר את דגל ההשבתה בממשק. חמישה תהליכים סווגו בטעות כפעילים על סמך

`cron`

  שאינו מסומן בהערה. **טבלת תזמונים שנגזרת מקבצים אינה אמינה מעצם בנייתה.**

- **חיפוש מחרוזת אינו רואה שם שמועבר במשתנה** — וגם לא שם שאין לו קיום כלל. פונקציה שמחזירה

`SETOF <table>`

  מעבירה כל עמודה בלי רשימת שדות בצד הלקוח, ואין שם לחפש. אותו דבר לגבי

`.select('*')`

  על

`view`

  שני הדפוסים חיים במערכת כרגע.

- **קובץ ששמו מסתיים ב־**

`_v2`

  **אינו בהכרח קובץ**

`V2`

`users_viewer_risk_prod_v2.html`

  קורא את טבלת ה־

`V1`

  **לקרוא את הקבוע, לא את שם הקובץ.**

- **ברירות מחדל של עמודה מתחזות לחתימת כותב.** העמודות שמזהות כותב הן אלה שאין להן ברירת מחדל והכותב ממלא במפורש.

- **עמודה שמותר בה ריק בתוך אינדקס ייחודי אינה מונעת כפילות.** שני ערכים ריקים אינם שווים ב־

`PostgreSQL`

- **תצוגה שנשענת על עמודה שנמחקה — נשברת.** תצוגות נערכות לפני עמודות, תמיד.

- **לפני הפניית הוראת דריסה לנתיב חדש — לוודא שהיעד ריק או אינו קיים.** הפניית הוראת הפלט לקובץ

`.rtf`

  השמידה מסמך בן 52,650 בתים שישב באותו נתיב.

- **אין לבנות נתיב על סמך פעולה שהמשתמש אמור היה לבצע.** לכתוב את התוכן ישירות, או לאמת שהמקור קיים.

- **פקודה שאושרה נשמרת מילולית ככלל הרשאה.** אישור "כן, אל תשאל שוב" לפקודה שמכילה סיסמה כותב את הסיסמה לדיסק, ב־

`.claude\settings.local.json`

  **אין לכתוב סיסמה בשורת פקודה.**

- **כלל התעלמות מסתיר סוד, אינו מסיר אותו.** ותבנית שנכתבה לשם קובץ מדויק אינה מכסה עותק

`.bak`

  שלו.

- **`Add-Content` אינו מוסיף מעבר שורה לפני התוכן.** אם הבית האחרון בקובץ אינו מעבר שורה, הרשומה החדשה נדבקת לשורה הקודמת. ב־

`.gitignore`

  זה שובר בשקט את הכלל שלפניו וגם לא מוסיף את החדש — ללא שגיאה.

---

## כלל שנוסף ב-8.8.2026 — עריכת פרומפטים

- **אין הזחות בתחילת שורה בפרומפטים.**

`upsert_prompt_from_sheet`

  מסירה רווחים בקצוות כל שורה, בתחילתה ובסופה, בכניסה. מוסכמה קבועה מכאן והלאה.

---

## מגבלות ידועות

`Claude Code`

רואה את עץ הקבצים בלבד. **אין לו ראייה לסכמה החיה.** יש להצליב בשאילתה ישירה.

הוא גם אינו רואה את **מצב ההפעלה** של תהליכי

`GitHub Actions`

ואינו רואה הגדרת

`view`

שאינה שמורה במאגר.

`Claude Code`

מוסיף שורת

`Co-Authored-By`

להודעת כל קומיט, מעבר לנוסח שניתן.

`Supabase SQL Editor`

אינו מריץ בלוק המשלב

`DDL`

עם גוף פונקציה בציטוט דולר.

`BEGIN`

ו־

`COMMIT`

חייבים לרוץ באותו בלוק ביצוע.

`Supabase SQL Editor`

מזריק

`limit 100`

לשאילתות, לעיתים בתוך רשימת

`IN`

— ומייצר שגיאת תחביר שאינה בשאילתה. הפתרון:

`No limit`

, או פיצול לשאילתות ללא

`IN`

`pg_get_functiondef()`

נכשלת על פונקציות צבירה. יש לסנן

`prokind in ('f','p')`

לפני הקריאה.

הודעת

`Docker is not running`

בפריסה אינה כשל. סימן ההצלחה:

`Deployed Functions on project`

ב־

`PowerShell`

מחרוזות עם

`@`

עלולות להשתבש. לתקן לפני הדחיפה.

`GitHub Pages`

מגיש מטמון ישן גם לאחר רענון מאולץ. לאמת את חותמת הגרסה בכותרת מול ה־

`SHA`

שנדחף לפני כל מסקנה על ה־

`Viewer`

הכלי

`gh`

אינו זמין במסוף

`VS Code`

— להשתמש ב־

`PowerShell`

**מיסוך:** העמודה

`phone`

מכילה ערכים ממוסכים, וטלפון קצר מששה ספרות נשמר גולמי. השוואה למספר מלא תחזיר אפס שורות **בשקט**. זו לא תיאוריה —

`set_disclaimed_agreed`

שבורה בדיוק כך.

---

## נתוני בדיקה — כלל פרשנות

**כל המטופלים הם נתוני בדיקה.** אין נתוני מטופלים אמיתיים.

**מצב אחרי הניקיון של 30.7:** שש זהויות. שלוש נשמרו בכוונה — דינה, חיים, משה. שלוש אינן ניתנות למחיקה בשל יומן הביקורת.

דינה מחזיקה 100 הפעלות ואת רוב ממצאי הסיכון. **אין להסיק מספירות שורות שום מדד על שימוש.**

שורות ישנות עשויות לחסר עמודות — היעדר ערך אינו באג.

---

## הוראת פלט

כל דוח או פלט ארוך — **באנגלית בלבד**, למניעת בעיות קידוד ותצוגה דו-כיוונית.

לכתוב לקובץ:

```
C:\Users\User\Documents\landbot-runner-clean\powershell_text\POWERSHELL_TXT.rtf
```

במצב דריסה, בקידוד

`UTF-8`

לאשר את הכתיבה אוטומטית. לאחר הכתיבה — לקרוא את הקובץ בפקודה נפרדת ולאמת שאינו ריק.

**אין להוסיף לפלט הצעות פעולה, שאלות המשך, או אישורי הבנה.** דיווח בלבד.

**חריג יחיד:** כאשר הבדיקה עצמה עוסקת בקובץ הזה — הכתיבה תדרוס את הראיה. במקרה כזה בלבד, ורק בבקשה מפורשת, הפלט יישאר בצ'אט.

---

## יומן הסשן

`docs\SESSION_LOG.md`

יומן גולמי, **מוסף בלבד**. אין לערוך רשומה קיימת ואין למחוק. טעות מתוקנת ברשומה חדשה שמפנה לקודמת.

הכתיבה דרך

`tools\log_append.ps1`

שמוסיף חותמת זמן ומדפיס את מספר השורות לפני ואחרי.

**הקובץ מתרוקן ידנית** — ורק אחרי שכל מה שרלוונטי בו זוקק אל

`SESSION_HANDOFF.md`

**שני הקבצים אינם תחת מעקב**

`Git`

---

## תחזוקת Git — פתוח

**קובץ זה עצמו אינו תחת מעקב**

`Git`

בניגוד לכלל שקובע לעבוד תמיד דרכו. גם

`docs\SESSION_LOG.md`

,

`tools\log_append.ps1`

, כלי ההרצה והפרומפטים אינם במעקב. פריטים 48 עד 50 במדריך.
