name: SUPA precheck (users_total.processed)

on:
  workflow_dispatch:
    inputs:
      limit:
        description: "כמה שורות להציג (ברירת מחדל 20)"
        required: false
        default: "20"
        type: string
      order_by:
        description: "(אופציונלי) סדר מיון למשל updated_at.desc"
        required: false
        type: string

permissions:
  contents: read
  actions: write

jobs:
  check-and-print:
    runs-on: ubuntu-latest
    steps:
      - name: Ensure jq is available
        shell: bash
        run: |
          if ! command -v jq >/dev/null 2>&1; then
            sudo apt-get update -y
            sudo apt-get install -y jq
          fi

      - name: Build REST URL (users_total, processed in NEW/IN_PROGRESS)
        id: build
        shell: bash
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        run: |
          set -euo pipefail
          if [[ -z "${SUPABASE_URL:-}" ]]; then
            echo "❌ Secret SUPABASE_URL חסר"; exit 1
          fi

          LIMIT="${{ github.event.inputs.limit }}"
          ORDER_BY="${{ github.event.inputs.order_by }}"

          BASE="${SUPABASE_URL%/}/rest/v1/users_total"
          QS="select=name,processed&limit=${LIMIT}"
          QS="${QS}&processed=in.(NEW,IN_PROGRESS)"
          if [[ -n "${ORDER_BY}" ]]; then
            QS="${QS}&order=${ORDER_BY}"
          fi

          echo "url=${BASE}?${QS}" >> "$GITHUB_OUTPUT"

      - name: Query Supabase and print field values (name + processed)
        id: query
        shell: bash
        env:
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          set -euo pipefail
          if [[ -z "${SUPABASE_KEY:-}" ]]; then
            echo "❌ Secret SUPABASE_KEY חסר"; exit 1
          fi

          URL="${{ steps.build.outputs.url }}"
          echo "🔎 GET $URL"

          RESP="$(curl -sS \
            -H 'apikey: '"$SUPABASE_KEY" \
            -H 'Authorization: Bearer '"$SUPABASE_KEY" \
            -H 'Accept: application/json' \
            "$URL")"

          echo "----- JSON גולמי -----"
          echo "$RESP"
          echo "-----------------------"

          COUNT="$(printf '%s' "$RESP" | jq 'length')"
          echo "📊 נמצאו ${COUNT} שורות עם processed ב- {NEW, IN_PROGRESS}"
          echo "count=${COUNT}" >> "$GITHUB_OUTPUT"

          if [[ "$COUNT" -gt 0 ]]; then
            echo "🔔 חלוקה לפי ערך:"
            printf '%s' "$RESP" | jq -r '.[].processed' | sort | uniq -c
            echo ""
            echo "🧾 פירוט (name → processed):"
            printf '%s' "$RESP" | jq -r '.[] | "\(.name) → \(.processed)"'
          else
            echo "⏭️ אין שורות מתאימות."
          fi

      # מפעיל את ה־workflow הראשי לפי *נתיב הקובץ* (ללא תלות ב־peter-evans)
      - name: Trigger RUN USERS_TOTAL (find-pending-users.yml) when count > 0
        if: ${{ steps.query.outputs.count != '0' }}
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const WORKFLOW_FILE = ".github/workflows/find-pending-users.yml";
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: WORKFLOW_FILE,
              ref: "main",
              inputs: {}
            });
            core.notice(`Triggered workflow: ${WORKFLOW_FILE}`);

      - name: Skip note (no pending rows)
        if: ${{ steps.query.outputs.count == '0' }}
        run: echo "⏭️ אין שורות מתאימות – ה-Workflow הראשי לא הופעל."
