name: Pushover Notify (Israel Time)

on:
  schedule:
    # Israel winter time (UTC+2):
    # 10,14,18,22 Israel  ==>  8,12,16,20 UTC
    - cron: "0 8,12,16,20 * * *"

  workflow_dispatch:
    inputs:
      message:
        description: "Message to send (optional)"
        required: false
        default: ""

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Build message
        shell: bash
        run: |
          set -euo pipefail

          INPUT_MSG="${{ github.event.inputs.message }}"
          if [ -n "${INPUT_MSG}" ]; then
            MSG="${INPUT_MSG}"
          else
            MSG="Scheduled notification (Israel time) @ $(date -u +'%Y-%m-%d %H:%M UTC')"
          fi

          echo "MSG<<EOF" >> "$GITHUB_ENV"
          echo "${MSG}" >> "$GITHUB_ENV"
          echo "EOF" >> "$GITHUB_ENV"

      - name: Pushover notify (optional second phone)
        env:
          PUSHOVER_APP_TOKEN:  ${{ secrets.PUSHOVER_APP_TOKEN }}
          PUSHOVER_USER_KEY:   ${{ secrets.PUSHOVER_USER_KEY }}
          PUSHOVER_USER_KEY_2: ${{ secrets.PUSHOVER_USER_KEY_2 }} # אופציונלי
          MSG:                 ${{ env.MSG }}
        shell: bash
        run: |
          set -euo pipefail

          if [ -z "${PUSHOVER_APP_TOKEN:-}" ] || [ -z "${PUSHOVER_USER_KEY:-}" ]; then
            echo "Missing required secrets"
            exit 1
          fi

          recipients=("${PUSHOVER_USER_KEY}")

          # טלפון נוסף – רק אם הוגדר
          if [ -n "${PUSHOVER_USER_KEY_2:-}" ]; then
            recipients+=("${PUSHOVER_USER_KEY_2}")
          fi

          for user_key in "${recipients[@]}"; do
            curl -sS --fail-with-body -X POST https://api.pushover.net/1/messages.json \
              -d token="${PUSHOVER_APP_TOKEN}" \
              -d user="${user_key}" \
              -d title="GitHub Actions" \
              -d message="${MSG}" >/dev/null
          done

          echo "Sent to ${#recipients[@]} device(s)"
