# מדריך מבנה הפרויקט — BOT

**תאריך אימות: 30 ביולי 2026**

**מעמד המסמך:** מחליף את הוראות הפרויקט הקודמות ואת שלושת קובצי התיאור הישנים.

---

## 0. כללי קריאה

**מאומת** = נבדק בשאילתה ישירה מול המסד, בפקודת מערכת, או בקריאת קוד.

**לא אומת** = מופיע בתיעוד קודם או משוער, וטרם נבדק. מסומן במפורש.

אין להציג פריט שאינו מאומת כעובדה.

---

## 1. שלוש תת־מערכות

בפרויקט חיות שלוש מערכות נפרדות באותו מאגר. הבלבול בתיעוד הקודם נבע מכך שכל קובץ תיאר אחת מהן בלבד.

| # | שם | תפקיד | מצב |
|---|---|---|---|
| א | צינור הנתונים | גיליון ← מסד ← שיחות ← סיכום ← סיכון ← צפייה | חי ופעיל |
| ב | מסלול התשובה בזמן אמת | יצירת תשובת המטפל ותיקונה | חי, בבוט הטיפולי |
| ג | סביבת הכוונון | הרצת שיחות בדיקה לשיפור פרומפטים | בדיקות בלבד |

**קבוצה ג' מנותקת מייצור.** התיקייה

`bot simulation`

אינה נוגעת בנתוני מטופלים. אומת שאין בה שום קריאה ל־

`users_tzvira_v2`

או ל־

`risk_reviews_v2`

---

## 2. שני הבוטים

| בוט | מסלול התשובה | פרומפט |
|---|---|---|
| הבוט הטיפולי | `runtime-corrected-response` | `therapist` |
| מדריך הקורס | קריאה ישירה ל־OpenAI | `course_guide` |

בטבלת ההפעלות קיימים שני ערכי מקור:

`A`

ו־

`B`

**לא אומת** איזה ערך שייך לאיזה בוט.

---

## 3. מודל הזהות

המזהה העסקי היחיד של המטופל:

`patient_code`

מסוג

`uuid`

מספר הטלפון משמש לזיהוי ראשוני בלבד.

### ארבעת הייצוגים

| ייצוג | תיאור |
|---|---|
| `patient_code` | מזהה פנימי, ללא מידע אישי |
| `phone_hash` | SHA-256 של הטלפון המנורמל. אינו הפיך |
| `phone_enc` | הטלפון המלא, מוצפן |
| `phone` | טלפון ממוסך, למשל `054***798` |

### הצפנה — מאומת

אלגוריתם:

`pgp_sym_encrypt`

עם

`aes256`

פורמט אחסון: הקידומת

`db1:`

ואחריה

`Base64`

המפתח נשמר ב־

`app_config`

תחת המפתח

`crypto_key_b64`

**אומת (30.7.2026):** כל השורות ב־

`patient_identity_map`

נושאות את הקידומת בעמודות המוצפנות. לפני הניקיון היו 39 שורות; אחריו נותרו שש.

**אומת:** בכל 39 השורות שנבדקו,

`phone_enc`

היה מאוכלס ללא יוצא מן הכלל. שלוש שורות היו ללא

`name_enc`

— ולשלושתן גם לא היה שם גלוי, ולכן לא היה פער נתונים.

---

## 4. טבלאות — מצב מאומת

### 4.1 כלל הזיהוי

**טבלה שקיימת לה מקבילה בסיומת** `_v2` **— ישנה ואינה בשימוש.**

הכלל חל על טבלאות בלבד. **הוא אינו חל על משימות תזמון ועל תהליכי**

`GitHub Actions`

שם רצות במקביל גרסאות ישנות וחדשות — ראו סעיפים 7 ו־8.

### 4.2 טבלאות פעילות

| טבלה | תפקיד | שורות |
|---|---|---|
| `patient_identity_map` | שכבת הזהות וההצפנה | 6 |
| `users_information_v2` | הרשאות והוראות לפי מטופל | 4 |
| `conversations_session_v2` | פתיחת שיחה ומזהה שיחה | 130 |
| `conversations_prod_v2` | הודעות שיחה פעילה — טבלת מעבר | 0 |
| `users_total_v2` | פרופיל מצטבר, שורה אחת למטופל | 5 |
| `users_tzvira_v2` | תמונת מצב לפי זמן | 11 |
| `risk_reviews_v2` | ממצאי סיכון | 14 |
| `process_queue_v2` | תור עיבוד | — |
| `conversation_events_v2` | יומן ביקורת, Append Only | 330 |
| `prompt_information_v2` | פרומפטים | 6 |
| `psychologists_v2` | פסיכולוגים | — |
| `risk_phrases` | ביטויי סיכון | — |
| `app_config` | מפתח ההצפנה | — |
| `talk_read_flags` | סימוני קריאה — **ללא סיומת. ראו 15.4** | 2 |

**תוקן 31.7.2026 ערב.** סעיף זה מנה קודם

`talk_read_flags_v2`

כפעילה, בסתירה לסעיף 15.4. **הסתירה הוכרעה לטובת 15.4** על שלוש ראיות בלתי תלויות:
כתיבה אחרונה (24.7–27.7 מול 29.6), שלוש מדיניות מסוננות על הטבלה חסרת הסיומת מול
שתי מדיניות פתוחות על גרסת ה־

`_v2`

וקוד — שלושת קובצי ה־

`Viewer`

מציבים

`FLAGS_TABLE`

לשם חסר הסיומת. **אין קובץ במאגר שמפנה ל־**

`talk_read_flags_v2`

בשליפה, בכתיבה או בסינון.

**כל המטופלים הם נתוני בדיקה.** אין במערכת נתוני מטופלים אמיתיים. אין להסיק מספירות שורות שום מדד על שימוש.

**ספירות אלה נכונות ל־30.7.2026, אחרי ניקיון טבלת הזהות** — ראו סעיף 21.1. לפני הניקיון היו 39 זהויות; נמחקו 33.

### 4.3 conversations_prod_v2 — הבהרה

התיעוד הקודם הגדיר אותה כמקור האמת. **ההגדרה מבוטלת.**

הטבלה **מתרוקנת בכל מחזור העברה**. היא מחזיקה שיחות פעילות בלבד. מקור האמת ההיסטורי הוא

`users_total_v2`

### 4.4 ההבחנה בין שתי טבלאות ההיסטוריה

`users_total_v2`

— 19 עמודות. מצב נוכחי אחד למטופל. מכילה שדות תור ושדות סיכון.

`users_tzvira_v2`

— 9 עמודות. תמונת מצב לפי

`time_key`

יחד עם

`patient_code`

אין בה שדות תור ואין בה שדות סיכון.

### 4.5 ההסבה — מצב חלקי

`users_tzvira_v2`

— עמודות הצל

`name`

ו־

`phone`

**נמחקו**

`risk_reviews_v2`

— עמודות הצל **עדיין קיימות**

זהו מקור התקלה המתוארת בסעיף 14.

### 4.6 טבלאות ישנות — ממתינות לפירוק

**עודכן 31.7.2026 ערב.** שבע טבלאות קיבלו סיומת

`_old_to_delete`

בשינוי שם, לא במחיקה. השחזור הוא פקודת

`rename`

הפוכה.

| טבלה מקורית | מצב |
|---|---|
| `users_information` | שם שונה. **נושאת עדיין שלוש מדיניות `anon` פתוחות** |
| `users_information_phone_backup` | שם שונה |
| `conversations_prod` | שם שונה. 0 שורות. הטריגר שלה הוסר |
| `conversations_prod_gibuy` | שם שונה |
| `risk_reviews` | שם שונה. המבט `risk_reviews_view` נשבר איתה |
| `users_tzvira` | שם שונה |
| `supa_guarded_run_log_details` | **שם שונה 31.7 ערב.** 485 שורות עם טלפון ושם מלאים. הצרכן היחיד היה `supa_daily_migration_guarded`, המופעל ממשימה 38 המושבתת |

**טרם טופלו:**

`process_queue`

`users_total`

`users_total_postprocess_queue`

`users_total`

הייתה המקור של

`get_all_phones_and_talks`

**החשיפה נסגרה ב-31.7 ערב** — שינוי שם ואחריו

`revoke`

ולכן שינוי שמה של

`users_total`

אינו חסום עוד. 21 שורות עם טלפון ושם, שניהם

`NOT NULL`

### 4.7 קובצי המיגרציה אינם מתארים את המסד — 31.7.2026 ערב

התיקייה

`supabase\migrations`

**אינה מקור אמת ואינה ארכיון.**

`20260518_v2_isolated_runtime.sql`

אינו כולל את

`conversation_id`

,

`status`

ו־

`severity`

ששלושתן קיימות בפועל ב־

`risk_reviews_v2`

ונכתבות על ידי הקוד. כלומר הקוד נכון והמיגרציה מיושנת.

בנוסף, שני אובייקטים שטופלו ב-31.7 ערב —

`get_all_phones_and_talks`

ו־

`supa_guarded_run_log_details`

— **אין להם משפט**

`CREATE`

בשום מקום בעץ הקבצים. הם נוצרו ישירות במסד.

**אין לקרוא מקובץ מיגרציה מבנה של טבלה חיה.** נדרשת שאילתה ישירה, תמיד.

---

## 5. פונקציות המסד

### 5.1 עומסי יתר כפולים — סיכון פעיל

שלוש פונקציות קיימות ביותר מחתימה אחת. מנוע המסד בוחר לפי סוגי הפרמטרים, ולכן קריאה עלולה להגיע לגרסה ישנה **בלי שתתקבל שגיאה**.

**`start_conversation_v2`** — **תוקן (27.7.2026).** נשארו שתי חתימות תקינות:

`(p_patient_code uuid)`

`(p_phone text, p_name text, p_source text)`

**מבוטל:** הקביעה שהחתימה בת שני הפרמטרים "חוזרת בכל פריסה". לא היה מנגנון שמייצר אותה מחדש. היא הייתה שריד ישן; המחיקה הקודמת נכשלה בשקט משום שלא צוינה החתימה המדויקת. הגרסה בת שני הפרמטרים חפפה לגרסת שלושת הפרמטרים (שבה

`p_source`

הוא

`DEFAULT NULL`

) וגרמה לשגיאת

`PGRST203`

. נמחקה סופית לפי חתימה מלאה, ואומת בקריאה חיה שההתנגשות נפתרה.

**`insert_conversation_v2`** — **תוקן (31.7.2026).** נותרה חתימה אחת:

`(p_phone text, p_conversation_id uuid, p_question text, p_answer text)`

**מבוטל:** הקביעה שמזהה שיחה עלול להירשם בשדה השם.

`PostgREST`

בוחר חתימה לפי **שמות המפתחות** בגוף

`JSON`

הבוט שולח ארבעה מפתחות בשמם, ובהם

`p_conversation_id`

— קבוצה שתואמת רק את חתימת ה־

`uuid`

השם

`p_name`

אינו נשלח כלל.

**הסיכון האמיתי היה אחר:** שני הקוראים השתמשו בשתי חתימות שונות, ולכן אף אחת מהן לא הייתה "המיותרת". הקורא השני היה הטריגר

`trg_mirror_conversation_to_v2`

על

`conversations_prod`

, שקרא במיקום והעביר

`new.name`

— כלומר הגיע לחתימת ה־

`text`

הוסרו לפי הסדר: הטריגר, הפונקציה

`mirror_conversation_to_v2`

, ואז חתימת ה־

`text`

לפי חתימה מלאה.

`conversations_prod`

אומתה כריקה לפני כן, ואומת חי לאחר מכן שהבוט כותב שיחות.

**`supa_daily_migration`** — **עומס שהתגלה ב-31.7 ולא היה מתועד.** שתי חתימות:

`(p_phone text)`

וללא ארגומנטים. חמישה עוטפים קוראים לה. חלק משרשרת

`V1`

שהושבתה.

**`upsert_users_information_v2_from_sheet`** — **תוקן (30.7.2026).** החתימה בת שישה הפרמטרים נמחקה לפי חתימה מלאה. נותרה חתימה אחת בלבד, בת שבעה פרמטרים, הכוללת את

`p_psychologist`

**אומת בקוד לפני המחיקה:** הפונקציה

`supa-sync`

שולחת תמיד את כל שבעת הפרמטרים בשמם, כולל

`p_psychologist`

גם כשערכו ריק. לכן החתימה בת השישה לא הייתה נגישה כלל מהמסלול החי, ומחיקתה לא שברה דבר.

**`upsert_psychologist_from_sheet`** — ארבע מול שש. הפרמטרים הנוספים:

`p_organization`

`p_text`

### 5.2 פונקציות מפתח

| פונקציה | תפקיד |
|---|---|
| `get_or_create_patient_code` | איתור או יצירת זהות |
| `start_conversation_v2` | פתיחת שיחה |
| `insert_conversation_v2` | רישום הודעה |
| `supa_daily_migration_guarded_v2` | בדיקת סיום שיחה |
| `supa_migrate_one_phone_v2` | העברה לפרופיל |
| `get_prompt_v2` | שליפת פרומפט |
| `upsert_prompt_from_sheet` | כתיבת פרומפט מהגיליון |
| `log_conversation_event_v2` | כתיבה ליומן הביקורת |
| `trace_patient_flow_v2` | מעקב מסלול מטופל |

### 5.3 תשתית סינון מוכנה ולא מחוברת

`current_psychologist_phone`

`current_user_is_admin`

`psychologist_names_for_viewer`

**מבוטל (30.7.2026):** הכותרת "מוכנה ולא מחוברת". שלוש הפונקציות **מחוברות ופעילות**.

שתי הראשונות הן בסיס כל שמונה מדיניות ה־

`RLS`

וגם של הסינון ב־

`rpc_get_users_tzvira_v2_viewer`

**אומת בקריאת גוף הפונקציות:**

`current_user_is_admin`

עטופה ב־

`COALESCE(..., false)`

ולכן בהיעדר אסימון היא מחזירה שקר. התנאי אינו ריק.

`current_psychologist_phone`

מחזירה את

`p.phone`

מתוך

`psychologists_v2`

— אותה עמודה שאליה משווים. התאמת הפורמט מובטחת מבנית.

שתיהן מזהות את המשתמש לפי

`auth.jwt() ->> 'email'`

**אומת שהאסימון אכן נושא מייל:** הפונקציה

`verify-login`

שולפת את המייל מ־

`psychologists_v2`

לפי הטלפון, ומריצה

`verifyOtp`

עם

`type: "email"`

הטלפון הוא מפתח חיפוש בלבד; זהות האימות היא המייל.

### 5.4 פונקציית הפענוח — חוברה ואובטחה (30.7.2026)

`rpc_get_users_tzvira_v2_viewer`

חתימה אחת, אפס ארגומנטים. מפענחת בצד השרת את

`name_enc`

ואת

`phone_enc`

ומחזירה שבע עמודות:

`id, patient_code, time_key, phone, name, last_talk_tzvira, summarized_linked_talk`

**מבוטל:** הקביעה שה־

`Viewer`

אינו קורא לה. הוא קורא לה מאז 30.7.2026, בשורה 590, במקום השליפה הישירה מטבלת הזהות.

**חשיפה שנמצאה ונסגרה.** לפני התיקון הפונקציה הייתה

`SECURITY DEFINER`

**ללא שום סינון**, ועם הרשאת הרצה ל־

`PUBLIC`

ול־

`anon`

כלומר קריאה אחת עם המפתח המוטמע בדף הייתה מחזירה את כל השמות והטלפונים מפוענחים, **בלי התחברות כלל** — עוקפת גם את שער האימות וגם את שמונה המדיניות.

**התיקון, בשני שלבים:**

הרשאת ההרצה נשללה מ־

`PUBLIC`

, מ־

`anon`

ומ־

`authenticated`

, ואומת שלא הייתה נסיגה לברירת מחדל פתוחה.

לגוף הפונקציה נוסף תנאי הסינון **הזהה** לזה שבמדיניות, כדי שלא תיווצר שכבת הרשאות שנייה בעלת היגיון משלה. רק אז הוחזרה ההרשאה ל־

`authenticated`

בלבד.

**התנהגות שיש להכיר:** הפונקציה מצטרפת אל

`users_tzvira_v2`

ולכן מחזירה זהות רק למטופל שיש לו שורה שם.

**פער שלא אומת:** האם

`.range()`

חל על קריאת

`rpc`

בגרסת הספרייה שבשימוש. השליפה הקודמת עברה עימוד של 1000 שורות; קריאת

`RPC`

כפופה למגבלת ברירת המחדל של

`PostgREST`

חסר משמעות בהיקף הנוכחי, **מהותי עם מטופלים אמיתיים.**

### 5.5 סכמה נוספת

קיימת סכמה בשם

`admin`

ובה פונקציה אחת:

`run_landbot_and_log`

**תוכנה לא נסקר.**

---

## 6. פונקציות Edge

12 פונקציות, כולן פרוסות ופעילות.

| פונקציה | גרסה | עודכן | מעמד |
|---|---|---|---|
| `runtime-corrected-response` | 33 | 15.7.2026 | הבוט הטיפולי |
| `supa-sync` | 42 | 20.7.2026 | סנכרון מגיליון |
| `psychologist-sync` | 7 | 20.7.2026 | סנכרון פסיכולוגים |
| `prompt-sync` | 6 | 15.7.2026 | סנכרון פרומפטים |
| `start-login` | 55 | 20.7.2026 | הזדהות |
| `verify-login` | 51 | 20.7.2026 | אימות |
| `trigger-process-worker` | 28 | 2.7.2026 | הפעלת עיבוד |
| `bot-monitor` | 42 | 4.7.2026 | ניטור |
| `crypto-tool` | 46 | 29.1.2026 | לא אומת |
| `secure-users-information` | 60 | 27.1.2026 | לא אומת |
| `risk_engine` | 59 | 17.12.2025 | הוחלף, עדיין פרוס |
| `openai-completion` | 57 | 27.10.2025 | לא אומת |

פקודת פריסה:

```
supabase functions deploy <name> --project-ref qcwimczsiuxkarwfiyai --no-verify-jwt
```

הדגל

`--no-verify-jwt`

נדרש משום שהאימות נעשה בכותרת

`x-landbot-secret`

**אין פונקציה בשם** `risk-scan`. הוא רץ ב־

`GitHub Actions`

בלבד.

---

## 7. תזמון — pg_cron

עשר משימות. **שלוש נכשלות ב־100%.**

| # | שם | תדירות | מצב |
|---|---|---|---|
| 1 | `daily_run_landbot` | יומי | **כשל מלא** |
| 11 | `landbot_message_sender_1645` | יומי | **כשל מלא** |
| 23 | `supa_run_log_cleanup_daily_0830` | יומי | תקין |
| 24 | `run_landbot_yes_hourly_15` | שעתי | **כשל מלא** |
| 25 | `run_100_link_talk_api_hourly_15` | שעתי | תקין |
| 27 | `run_100_link_talk_batch_hourly_15` | שעתי | תקין |
| 28 | `reset_users_total_stuck` | שעתי | תקין |
| 38 | `guarded-05-20-35-50` | כל 15 דקות | מושבת (active=false) — אומת 1.8.2026 |
| 40 | `guarded-v2-05-20-35-50` | כל 10 דקות | תקין |
| 41 | `users_information_v2_daily_incremental_backup` | יומי | תקין |

### סיבות הכשל — מאומתות

**משימה 1** — שגיאת תחביר. הפקודה

`perform`

חוקית רק בתוך

`plpgsql`

**משימה 11** — הפונקציה

`landbot_message_sender`

**אינה קיימת**. נמחקה.

**משימה 24** — שגיאת תאימות לאחר שדרוג

`pg_net`

הפונקציה

`net.http_collect_response`

מחזירה כיום טיפוס מורכב בעל שם, ולכן רשימת העמודות המפורשת פסולה. הכשל בשלב איסוף התשובה, **אחרי** שליחת הבקשה.

### הערות

משימות 1 ו־11 הן שרידים של תכונה שפורקה. אין מה לתקן בהן.

שמות משימות 38 ו־40 מטעים: אף אחת מהן אינה רצה בדקות המצוינות בשם.

---

## 8. GitHub Actions

21 תהליכים. **חמישה מושבתים** — תוקן 31.7.2026 ערב.

> **אזהרה מבנית.** מצב ההפעלה של תהליך **אינו נמצא בקובץ**.
>
> `GitHub Actions`
>
> שומר את דגל ההשבתה בממשק, ולכן קובץ שמכיל
>
> `cron`
>
> שאינו מסומן בהערה עשוי בהחלט להיות מושבת. **טבלת תזמונים שנגזרת מקבצים אינה
> אמינה מעצם בנייתה.** מקור האמת היחיד הוא לשונית
>
> `Actions`

### מושבתים — מאומת בממשק, צילום מסך 31.7.2026

`users_inform-checker`

`Process Queue Reconciler (hourly)`

`Process Queue Worker (event-ish)`

`Postprocess short_summarized`

`Risk-scan V2`

**הקובץ**

`risk-scan-v2.yml`

**עדיין מכיל בלוק**

`schedule:`

**עם**

`*/15 * * * *`

**שאינו מסומן בהערה.** זהו טקסט מת. הוא סתר את קומיט

`cf720d7`

שהצהיר על השבתה, והממשק הכריע לטובת הקומיט. יש לנקות את הבלוק כדי שהמאגר
יפסיק לסתור את המציאות.

### פעילים בגרסה 2

`Postprocess short_summarized V2`

`Process Queue Worker V2 (event-ish)`

`Process users_total_v2 V2 (NEW → DONE/ERROR)`

`Process Queue Reconciler V2`

### פעילים ישנים

`Run Landbot (v2) via reusable workflow`

`Run users_inform Bot (manual/dispatch only)`

`RUN USERS_TOTAL`

`Run users_total Bot (NOW)`

`SUPA precheck (users_total.processed)`

`Process users_total (NEW → DONE/ERROR)`

`OpenAI smoke test (risk scan)`

`OpenAI smoke test (risk scan NEW)`

`Pushover Manual Test (Python)`

`Pushover Notify (Israel Time)`

`Sanity Check (Manual)`

`pages-build-deployment`

---

## 9. שרשרת העיבוד — מאומתת מקצה לקצה

יומן הביקורת מוכיח את הרצף המלא:

`conversation_started`

← `users_total_updated`

← `process_queue_created`

← `postprocess_completed`

← `users_tzvira_updated`

### מנגנון סיום שיחה

שיחה נחשבת שהסתיימה כאשר עברו 10 דקות מההודעה האחרונה. **אומת 1.8.2026.**

**שם הפונקציה מטעה.** בגוף

`supa_guard_15m_preview_v2`

וגם בגוף

`supa_daily_migration_guarded_v2`

התנאי הוא

`>= 10.0`

ונימוק הדחייה הוא

`recent activity < 10m`.

הספרה 15 בשם היא שריד, לא הסף בפועל.

**אין רישום סיום מפורש.** בטבלת ההפעלות קיים

`started_at`

ואין

`ended_at`

**העמודה** `current_stage` **אינה מתעדכנת לעולם.** כל 127 הרשומות נמצאות בערך

`conversation_started`

### כניסה חוזרת

מטופל היוצא ונכנס שוב לפני שהשיחה הקודמת נאספה יוצר רשומת הפעלה חדשה. זו התנהגות ידועה.

**אומת 1.8.2026 — רשומת ההפעלה החדשה נותרת יתומה לצמיתות.**

`Landbot`

ממשיך לשלוח את מזהה השיחה הקודם, ולכן ההודעות שנכתבות אחרי הכניסה החוזרת נתלות במזהה הישן.

`insert_conversation_v2`

אינה ממציאה מזהה, אלא דורשת אותו מהקורא ומוודאת רק שהוא שייך למטופל — ולכן המזהה הישן מתקבל ללא שגיאה.

**76 מתוך 150 רשומות ב־**

`conversations_session_v2`

**נושאות**

`conversation_started`

**בלבד.** שמונה נוספות ללא אף אירוע. התיקון ב־

`Landbot`,

לא במסד.

### פערי יומן הביקורת

היומן **אינו מכסה את מערכת הסיכון.** אין בו אף אירוע מ־

`risk_reviews_v2`

או מ־

`risk_phrases`

### יומן הביקורת — Append Only נאכף (מאומת 27.7.2026)

הטבלה

`conversation_events_v2`

מוגנת בטריגר

`prevent_conversation_events_v2_change`

**מאומת בניסיון מחיקה חי:** המחיקה נחסמה בשגיאה

`conversation_events_v2 is append only`

היומן עמיד למחיקה כפי שיומן ביקורת צריך להיות — אחת מנקודות המבנה שעובדת כמתוכנן.

**מפתח זר מאומת:** קיים

`conversation_events_v2_conversation_fk`

מ־

`conversation_events_v2`

אל

`conversations_session_v2`

לכן אי אפשר למחוק שיחה שכבר נרשמה ביומן, בלי למחוק תחילה את רישום היומן — וזה חסום. **התוצאה: שיחה שנרשמה ביומן אינה ניתנת למחיקה כלל.**

---

## 10. מערכת הסיכון

### כותב אחד בלבד — מאומת 31.7.2026 ערב

כל 14 השורות ב־

`risk_reviews_v2`

נכתבו על ידי

`scripts\process_users_total_v2.mjs`

**ואף אחת** על ידי

`scripts\risk-scan.mjs`

ההוכחה היא בחתימת העמודות, לא בהשערה. בכל שורה קיים

`compare_status = PENDING`

שאין לו ברירת מחדל ולכן נכתב במפורש — וזו חתימת הכותב הראשון. ובכל שורה

`severity`

ריק, בעוד שהכותב השני **תמיד** ממלא אותו.

**שים לב:**

`status`

ו־

`match_method`

הם **ברירות מחדל של המסד**

`'NEW'`

ו־

`'1'`

ואינם חתימה של אף כותב. ברירת מחדל מתחזה בקלות לראיה.

הערך

`CHECKED`

נכתב מה־

`Viewer`

באישור מטפל. אומת חי: אישור שבוצע בסשן כתב

`reviewed_at`

ו־

`reviewer`

ולא נגע בשום שדה אחר.

### השרשרת אוטומטית — מאומת 31.7.2026 ערב

שיחת ניסוי שבוצעה **בלי שום הפעלה ידנית** יצרה פריט בתור ב-15:40:00, שהושלם
ב-15:40:20, ואחריו נכתבה שורת סיכון. זמן העיבוד 20 עד 24 שניות מכניסה לתור ועד סיום.

**מרווח התזמון אינו ידוע.** שתי ריצות מתוזמנות שנצפו היו במרחק 25 דקות זו מזו — מה
שאינו תואם לא לרבע שעה ולא לשעה, ושתי נקודות אינן מגדירות מחזור. נדרשות שלוש ריצות
רצופות ללא הפעלה ידנית ביניהן.

**זה אינו פריט קוסמטי.** המרווח קובע את ההשהיה בין רגע שבו מטופל מסיים שיחה לבין
הרגע שבו סיכון מופיע מול המטפל.

### שני מסלולים — שניהם מאומתים חיים (27.7.2026)

**מסלול המודל** — סימון

`match_method = 1`

המודל קורא את השיחה ומזהה מצוקה, כולל מרומזת ללא מילת מפתח.

**מסלול הביטויים** — סימון

`match_method = 2`

התאמה מילולית מול

`risk_phrases`

, בלי הבנה. תופס ביטוי מפורש שהמודל עלול לפספס.

**השניים משלימים, לא מחליפים.** שתי רשתות בגדלים שונים.

### מאומת בבדיקת קצה־לקצה

הוזרמה שיחת בדיקה חיה עם שני משפטים: אחד מרומז, ואחד המכיל ביטוי מהרשימה. שתי רשומות נכתבו — אחת ב־

`match_method = 1`

מהמודל, ואחת ב־

`2`

מהביטויים. **השרשרת המלאה עבדה אוטומטית:** טריגר ← תור ← עיבוד ← שתי הסריקות ← כתיבה.

זה מאשר גם שהטריגר במסד יורה, ושמסלול הביטויים כותב בפועל.

### מדוע לא היו קודם רשומות ב־2

**מבוטל:** הקביעה שהיעדר רשומות ב־

`match_method = 2`

הוא באג. הסיבה הפשוטה: לא היו ביטויים מהרשימה בשורות מטופל בנתונים הקיימים. ברגע שהוזרם ביטוי — נתפס מיד.

### היכן שני המסלולים רצים

שניהם באותו סקריפט:

`scripts/process_users_total_v2.mjs`

בפונקציה

`processOneRow`

, ברצף: מודל (שורה 853, ללא סימון), ואז ביטויים (שורות 860 עד 869, סימון 2). הסריקה הטיפשה מזהה שורת מטופל לפי הסמן

`q:`

— לא לפי שם, ולכן מחיקת עמודת השם לא נגעה בה.

### מי מפעיל את השרשרת

התהליך

`process-queue-worker-v2.yml`

, כל שעה, מריץ את

`process_queue_worker_v2.mjs`

שמפעיל את הסקריפט לעיל כתהליך־בן, לכל שורה בתור. **מקור התזמון הוא**

`GitHub Actions`

**, לא**

`pg_cron`

מנגנון התור בשלוש שכבות: טריגר במסד מכניס מיד; המהלך הראשי מעבד; רשת הביטחון

`reconcile-new-to-queue-v2`

תופסת נשמטים אם הטריגר החמיץ.

### risk-scan V2 — הושבת (27.7.2026)

היה **מימוש שלישי ומיותר** של מסלול הביטויים —

`scripts/risk-scan.mjs`

, תהליך נפרד שרץ כל 15 דקות. נשבר משום ששלף את העמודה

`name`

שנמחקה מ־

`users_tzvira_v2`

, וזיהה שורות מטופל בשיטה נחותה לפי שם במקום לפי

`q:`

**הושבת** ב־

`disabled_manually`

. לא נמחק ולא תוקן — היה כפילות שבורה, והמסלול המשולב מכסה את עבודתו במלואה.

**הקובץ עדיין מכיל בלוק** `schedule` **— ההסרה הסופית היא שלב נפרד.**

### כפילות שטרם הובררה — פחות דחופה

מסלול הביטויים קיים כעת בשני מקומות: בסקריפט המשולב הפעיל, ובקובץ

`risk-scan.mjs`

המושבת. לאחר ההשבתה, הכפילות הפעילה הוסרה.

### התראות

`Pushover Notify`

רץ ב־

`GitHub Actions`

, יומי ב־10:00. הקובץ

`tools/risk_reviews_notify.py`

**מידרדר בשקט** בעת חוסר בעמודה — מנסה שמות חלופיים במקום להיכשל. תקלה בו תהיה בלתי נראית.

**ניטור כשלים כללי:** קיים ניטור מובנה של

`GitHub`

ברמת החשבון — מייל אוטומטי לבעל המאגר על כל תהליך שנכשל. הוא מכסה תהליכי

`Actions`

בלבד, **לא** משימות

`pg_cron`

---

## 11. שכבת הצפייה

### ארבעה קבצי צפייה, לא שלושה — מאומת 31.7.2026 ערב

| קובץ | REVIEWS_TABLE | FLAGS_TABLE |
|---|---|---|
| `users_viewer_risk_prod.html` | `risk_reviews` — **V1** | `talk_read_flags` |
| `users_viewer_risk_prod_v2.html` | `risk_reviews` — **V1** | `talk_read_flags` |
| `users_viewer_tzvira_v2.html` | `risk_reviews_v2` | `talk_read_flags` |

**למרות שמו,**

`users_viewer_risk_prod_v2.html`

**קורא את טבלת ה־**

`V1`

רשימות השדות שלו זהות בית־לבית לקובץ ללא הסיומת, באותם מספרי שורות. **רק**

`users_viewer_tzvira_v2.html`

מגיע אל

`risk_reviews_v2`

בכלל. **לקרוא את הקבוע, לא את שם הקובץ.**

**קובץ רביעי, במעקב**

`Git`

`users_viewer_risk_v2.html`

מריץ

`.select('*')`

על

`conversation_events_v2_view`

בשורות 51 ו-206. אותו דפוס קיים ב־

`bot-monitor`

שורות 197 ו-198. שני צרכנים מושכים כל עמודה של אותה תצוגה בלי לנקוב בשם עמודה אחת,
ולכן **שום ביקורת לפי שם עמודה אינה רואה מה עובר דרכם.** הגדרת התצוגה אינה במאגר.

11 קובצי

`HTML`

| קובץ | טבלאות | שער אימות |
|---|---|---|
| `users_viewer_tzvira_v2.html` | V2 | יש |
| `users_viewer_risk_prod.html` | ישנות | יש |
| `users_viewer_risk_prod_v2.html` | **ישנות** | יש |
| `users_viewer_risk_v2.html` | `conversation_events_v2_view` | **אין — זמני במכוון** |
| `login_phone.html` | — | דף הזדהות |
| `login_phone_v2.html` | — | דף הזדהות |
| `launcher.html` | — | מפנה |
| `launcher_v2.html` | — | מפנה |
| `launcher2.html` | — | מפנה |
| `otp_test.html` | — | יתום |
| `monitor/index.html` | — | יתום |

### הקובץ הפעיל

`users_viewer_tzvira_v2.html`

הוא היחיד הפונה לטבלאות

`V2`

והיחיד הקורא ל־

`RPC`

**עודכן 30.7.2026 — שתי קריאות** `RPC`:

שורה 590 —

`rpc_get_users_tzvira_v2_viewer`

לזהות מפוענחת ומסוננת. **החליפה את השליפה הישירה של**

`patient_code, name, phone`

**מטבלת הזהות.**

שורה 622 —

`psychologist_names_for_viewer`

לשם המטפל.

יתר הגישות ישירות ל־

`PostgREST`

**נשמר בשינוי:** חוזה המוכנות של

`CACHE.loaded`

וטיפול הכשל הרך. כשל בפענוח משאיר את הממשק שמיש, ורק החיפוש לפי שם מושבת.

**הקבוע**

`IDENTITY_TABLE`

בשורה 288 אינו בשימוש עוד. הסרתו היא ניקוי נפרד.

**נותר גלוי ולא טופל:** שורה 559 עדיין שולפת

`name`

ו־

`phone`

מ־

`risk_reviews_v2`

— נשלחות לדפדפן ואינן מוצגות. וכן שורה 987 במסלול הנסיגה.

**אומת שהצינור סביב**

`data-name`

**ו־**

`data-phone`

**הוא קוד מת:** הן ניזונות מ־

`CACHE.allRows`

שמקורו ב־

`users_tzvira_v2`

, ושם אין עמודות אלה. הערכן

`undefined`

תמיד. **אין הדלפת שם ל־**

`talk_read_flags`

### שם מטעה

`users_viewer_risk_prod_v2.html`

שונה מהמקור בשלוש שורות בלבד **ופונה לטבלאות הישנות**. הסיומת בשם הקובץ בלבד.

### מפתח anon

מפתח זהה מוטמע בשישה קבצים. זהו מפתח פרסום לפי הגדרה, ולא דליפת סוד. בטיחותו נשענת כולה על

`RLS`

**החלפתו מחייבת שבעה תיקונים ידניים.**

### שער זמני

`users_viewer_risk_v2.html`

פועל ללא אימות **במכוון**, לצורכי בדיקה. מיועד לסגירה לפני ייצור.

---

## 12. סנכרון מגיליונות

### עיקרון

ה־

`Apps Script`

הוא שולח גנרי בלבד. אינו מכיר את מבנה היעד. כל ההיגיון חי ב־

`supa-sync`

וב־

`RPC`

### מסלול

גיליון ← `Apps Script` ← `supa-sync` ← `RPC` ← טבלה

### התנהגות

שורה מסומנת

`v`

בעמודה

`update`

נשלחת. חוזר

`DONE`

או

`FAIL`

או

`NO_PHONE`

### נקודת כשל שחייבים לזכור

עמודה שנשלחת ואינה קיימת ב־

`RPC`

**נזרקת בשקט**, וההחזרה עדיין תהיה

`DONE`

**חובה לאמת ישירות מול המסד** בכל הוספת עמודה.

בקוד קיימת הערה שהטבלה אינה מכילה

`name`

אך **אין סינון בפועל**. אומת בגיליון שקיימת עמודת

`name`

, ולכן השם אכן נשלח. **ההערה מטעה.**

### מבנה המשלוח — מאומת (30.7.2026)

ה־

`Apps Script`

בונה את גוף הבקשה **דינמית משורת הכותרות**. כל עמודה שאינה ריקה מועתקת, והטלפון נכפה תמיד.

הפונקציה

`supa-sync`

, לעומת זאת, שולחת ל־

`RPC`

**רשימה קבועה של שבעה פרמטרים בשמם**, כולל

`p_psychologist`

, גם כשערכו ריק. לכן החתימה הנבחרת אינה תלויה בגיליון.

**תא ריק:** ה־

`Apps Script`

משמיט אותו; ה־

`supa-sync`

שולח

`null`

מפורש.

### נתיב עקיפה רדום — לא טופל

בשורה 111 של

`supa-sync`

ברירת המחדל של שם הטבלה היא

`users_information`

**ללא סיומת**. בקשה שלא תציין טבלה לא תגיע לענף ה־

`RPC`

אלא לנתיב הגנרי, שמכניס כל עמודה ישירות לטבלה — **בלי**

`patient_code`

**, בלי הצפנה, ובלי**

`get_or_create_patient_code`

ה־

`Apps Script`

כן שולח את שם הטבלה, ולכן הנתיב רדום. **כל שגיאת הגדרה תפעיל אותו בשקט.**

### הכתיבה לטבלת הזהות — שונתה (30.7.2026)

הפונקציה

`upsert_users_information_v2_from_sheet`

כתבה עד כה את השם, הטלפון הממוסך והמייל ל**עמודות הגלויות** של טבלת הזהות.

היא כותבת כעת ל־

`name_enc`

ול־

`email_enc`

בלבד, בהצפנה זהה לזו של

`get_or_create_patient_code`

`phone_enc`

**אינו נכתב** — הטלפון הוא מפתח החיפוש, ושינוי שלו יוצר מטופל חדש ממילא.

**תוקן באותה הזדמנות:** ערך ריק **אינו דורס** עוד את הערך הקיים. קודם לכן

`name = v_name_clean`

נכתב ללא תנאי, ולכן תא שם ריק בגיליון מחק את השם.

**אומת חי:** שינוי שם בגיליון מתפשט לאורך כל השרשרת ומופיע ב־

`Viewer`

---

## 13. פרומפטים

הטבלה

`prompt_information_v2`

שש רשומות. עמודת התוכן:

`user_text`

— אותו שם כמו ב־

`users_information_v2`

מה שאיפשר העברה בלי שינוי קוד.

| מפתח | מזהה קודם | תיאור |
|---|---|---|
| `therapist` | 99999999 | הנחיות המטפל |
| `course_guide` | 88888888 | מדריך הקורס |
| `risk` | 77777777 | מציאת סיכון |
| `corrector` | 123456789 | הנחיות ה־Corrector |
| `pre_patient` | 66666666 | הנחיות נוספות |
| `summary` | 55555555 | סיכום שיחות |

**מבוטל:** הקביעה שפרומפט ה־

`Corrector`

נטען לפי רשומה עם

`phone = 123456789`

**חסר בטבלה:** אין עמודות גרסה, סטטוס פעיל, או סביבת עבודה. היעד של ניהול גרסאות וחזרה לאחור **לא מומש**.

---

## 14. מסלול התשובה בזמן אמת

### נקודת קצה

```
https://qcwimczsiuxkarwfiyai.supabase.co/functions/v1/runtime-corrected-response
```

שיטה:

`POST`

כותרת אימות:

`x-landbot-secret`

### תשעה שדות קלט — כולם חובה כמחרוזת

`prompt20`

`pre_patient20`

`patient20`

`summarized20`

`tzvira`

`response20`

`question20`

`patient_id`

`session_id`

שדה חסר מחזיר

`missing_required_field:<field>`

### שלבי העיבוד

`buildTherapistInstructions()`

← `buildCandidateInput()`

← `generateCandidate()`

← `runCorrector()`

### buildTherapistInstructions

מחברת שלושה שדות:

`prompt20`

`pre_patient20`

`patient20`

ומוסיפה כללים תפעוליים קבועים:

- תשובה בעברית בלבד
- שמירה על התאמת מגדר
- טקסט רגיל בלבד
- לכל היותר שאלה אחת
- לא לחזור על הצעה שנדחתה
- לא לחזור על אותה אמפתיה מהתשובה הקודמת
- להציע פעולה מעשית רק כאשר המטופל ביקש עזרה מעשית
- **כללי בטיחות גוברים על כל השאר**

### buildCandidateInput

מעבירה למודל המטפל, בפורמט מפורש עם שמות השדות:

| שדה | תוכן |
|---|---|
| `summarized20` | סיכום שיחה |
| `tzvira` | היסטוריה מצטברת שאושרה |
| `response20` | תשובת המטפל הקודמת |
| `question20` | הודעת המטופל הנוכחית |

### generateCandidate

קריאה ל־

`OpenAI Responses API`

עם

`max_output_tokens = 500`

`temperature = 0.7`

`store = false`

וכן

`patient_id`

ו־

`session_id`

כ־

`metadata`

כשל מחזיר

`candidate_generation_failed`

### runCorrector

מקבל חמישה שדות:

`therapist_instructions`

`accepted_prior_history`

`previous_accepted_therapist_response`

`current_patient_message`

`candidate_response`

פועל תחת

`no_look_ahead_contract`

— אינו רואה את המשך השיחה.

`temperature = 0.1`

`max_output_tokens = 700`

הפלט הנדרש:

```json
{"action": "PASS או REWRITE", "final_response": "טקסט", "reason_codes": []}
```

### תוצאות אפשריות

`PASS`

— התשובה נשמרת כפי שהיא

`REWRITE`

— מוחזרת תשובה מתוקנת

`FALLBACK`

— תקלה בתיקון, נשמרת תשובת המועמד

קיים

`alias`

בשם

`correction_action`

המקביל ל־

`corrector_decision`

**משימה פתוחה:** לקבוע שדה קנוני יחיד ולהסיר את הכפילות.

### קודי סיבה

`REPEATS_REJECTED_IDEA`

`VIOLATES_USER_CONSTRAINT`

`REDUNDANT_SUMMARY`

`NO_FORWARD_PROGRESS`

`UNSUPPORTED_INFERENCE`

`OVER_ANALYSIS`

`OVERLY_TASK_ORIENTED`

`TOO_LONG`

`CONTINUITY_ERROR`

`MISSES_DIRECT_REQUEST`

`TONE_MISMATCH`

`OTHER`

### מבנה התשובה — שלב בדיקות

`ok`

`answer`

`candidate_answer`

`corrected_answer`

`corrector_decision`

`correction_action`

`reason_codes`

`fallback_used`

`correlation_id`

**מצב זמני:** השדה

`answer`

מכיל כיום טקסט משולב — תשובה מקורית יחד עם התשובה לאחר הבדיקה. בייצור הוא אמור להכיל את התשובה הסופית בלבד, ויתר השדות יוסרו.

### לוגים

`correlation_id`

`candidate_success`

`corrector_decision`

`fallback_used`

`http_status`

`candidate_elapsed_ms`

`corrector_elapsed_ms`

`total_elapsed_ms`

וכן שני לוגי חריגה:

`candidate_generation_exception`

`openai_http_error`

### מודלים

מטפל:

`gpt-5.4`

מתקן:

`gpt-5.4`

ברירת מחדל בקוד אם משתני הסביבה חסרים.

### משתני סביבה

`OPENAI_API_KEY`

`LANDBOT_WEBHOOK_SECRET`

`THERAPIST_MODEL`

`CORRECTOR_MODEL`

### רכיבי Landbot — לא אומת מול החשבון

שמות הבלוקים בזרימה **לא אומתו**. מבנה המשתנים ידוע מהעבודה.

**בלוק הזדהות** — מקבל מספר טלפון, מזהה את המטופל, טוען את המידע שלו, מקשר ל־

`patient_id`

ושומר

`session_id`

**בלוק הווב־הוק** — שיטה

`POST`

, כותרות

`Content-Type: application/json`

ו־

`x-landbot-secret`

, גוף הבקשה הוא תשעת השדות, כל אחד כמחרוזת.

**מיפוי הפלט** —

`answer`

נכתב אל

`response20`

**חשוב:** אין לפרש עוד ישירות את מבנה

`OpenAI`

הגולמי, כפי שנעשה בעבר עם

`output.0.content.0.text`

הפונקציה כבר מפשטת את התוצאה.

**בלוק עדכון ההיסטוריה** — ההיסטוריה החדשה היא הקודמת, ועליה הודעת המטופל הנוכחית והתשובה שנבחרה.

**קריטי:** יש לצבור את התשובה שהתקבלה בפועל, כלומר

`corrected_answer`

— **לא** בהכרח

`candidate_answer`

**מצב נוכחי:** השדה

`summarized20`

נשלח כמחרוזת ריקה, אך **חייב להישלח**.

---

## 15. אבטחה — מצב מאומת

### 15.1 תצוגה מפוענחת חשופה — נמחקה (27.7.2026)

`patient_identity_map_decrypted`

**נמחקה.** לפני המחיקה: הוענקו לה כל ההרשאות ל־

`anon`

, והיא שלפה את מפתח ההצפנה מ־

`app_config`

בצירוף. ההגנה היחידה הייתה תקלת פענוח — הקידומת

`db1:`

מכילה נקודתיים, פסול ב־

`Base64`

, וכל 38 השורות נשאו אותה.

**מדוע נמחקה ולא תוקנה:** ההגנה הייתה תקלה בלבד. תיקון התצוגה, או שינוי פורמט ההצפנה, היו פותחים דליפה מלאה של שם, טלפון ומייל. אומת לפני המחיקה: אפס תלויות, אף פונקציה אינה מזכירה אותה, אף

`HTML`

אינו קורא לה.

**מאומת אחרי המחיקה:** האובייקט נעלם לחלוטין. חשיפת המידע הרפואי — סגורה בהרשאות, לא בתקלה.

### 15.2 דפוס ההרשאות

רוב טבלאות

`V2`

מוגדרות

`RLS ON`

**ללא אף מדיניות**. משמעות: גישה ישירה חסומה לחלוטין, וכל קריאה חייבת לעבור דרך

`SECURITY DEFINER`

או דרך

`service_role`

היוצאות מן הכלל הן בדיוק הטבלאות שה־

`Viewer`

קורא מהן.

### 15.3 סינון פסיכולוג ומטופל — קיים ופעיל

**מאומת: שמונה מדיניות על חמש טבלאות.** הסינון חי ב־

`RLS`

, לא ב־

`HTML`

שרשרת ההיתר:

`current_user_is_admin()`

**או** קיום שורה ב־

`users_information_v2`

שבה

`psychologist`

שווה ל־

`current_psychologist_phone()`

| טבלה | שליפה | עדכון | הוספה |
|---|---|---|---|
| `users_information_v2` | יש | — | — |
| `users_tzvira_v2` | יש | — | — |
| `patient_identity_map` | יש | — | — |
| `risk_reviews_v2` | יש | יש | — |
| `talk_read_flags` | יש | יש | יש |

**מבוטל:** הקביעה שקיימת מדיניות זמנית על טבלת הזהות. המדיניות היחידה שם היא מדיניות הסינון המלאה.

**מבוטל:** הקביעה שהסינון אינו יכול לחיות ב־

`RLS`

**פער שטרם אומת:** לשלוש הטבלאות הראשונות אין מדיניות כתיבה. ייתכן שזה מכוון — ה־

`Viewer`

קורא מהן בלבד.

### 15.4 טבלת הסימונים — הכלל אינו חל

מדיניות הסינון נוצרו על

`talk_read_flags`

**ללא סיומת**. זו גם הטבלה שה־

`Viewer`

קורא בשורה 287.

כלומר הישנה היא **הפעילה**, וגרסת

`_v2`

היא הנטושה — **הפוך מכלל הזיהוי שבסעיף 4.1.**

> **הוכרע 31.7.2026 ערב.** סעיף 4.2 מנה קודם את
>
> `talk_read_flags_v2`
>
> ברשימת הפעילות, בניגוד לסעיף זה. **הסעיף הזה הוא הנכון, ו-4.2 תוקן.**
>
> **הרגרסיה נסגרה.** הטבלה ששמה שונה ל־
>
> `talk_read_flags_old_to_delete`
>
> הוחזרה לשמה, וה־
>
> `Viewer`
>
> אומת חי מקצה לקצה. שלוש מדיניות הסינון שרדו את שינוי השם ולא אבד מידע.

### 15.5 חשיפה בטבלה הנטושה

על

`talk_read_flags_v2`

קיימות ארבע מדיניות. שתיים מהן פתוחות:

שליפה בתנאי

`auth.role() = 'authenticated'`

— כל משתמש מחובר רואה את כל השורות, ללא סינון פסיכולוג.

עדכון בתנאי

`true`

— ללא כל הגבלה.

זהו פיגום שנשאר מאחור. **הטבלה נטושה, ולכן החשיפה תיאורטית — אך יש להסירה.**

### 15.6 הרשאות הרצה — אינן מעידות על כוונה (מאומת 30.7.2026)

**כמעט כל פונקציה בסכמה**

`public`

**מעניקה הרצה ל־**

`anon`

**ול־**

`PUBLIC`

זו ברירת המחדל של

`PostgreSQL`

: פונקציה חדשה מקבלת

`EXECUTE`

ל־

`PUBLIC`

אוטומטית.

**המסקנה המעשית:** אין להסיק מהרשאה שפונקציה "נועדה" להיקרא מבחוץ, ואין להסיק שפונקציה "נפתחה בכוונה". היא פשוט מעולם לא נסגרה.

**ההשלכה החמורה:** כל פונקציה

`SECURITY DEFINER`

שאינה מסננת בגופה היא חשיפה פוטנציאלית מלאה, גם בלי שאיש פתח לה דבר. הסינון חייב לחיות **בגוף הפונקציה**, ולא להישען על הרשאות.

### 15.7 שלוש פונקציות נגישות ל־anon — נסקרו (31.7.2026)

`decrypt_user_text(p_phone text)`

**מבוטל:** החשש שהיא מפענחת קלט חופשי. היא מקבלת טלפון, שולפת

`user_text_enc`

מ־

`users_information`

ומפענחת שורה אחת בלבד. אינה מנגנון פענוח כללי.

**החשיפה בפועל:**

`SECURITY DEFINER`

ללא סינון, פתוחה ל־

`anon`

— מי שמחזיק במפתח ובמספר טלפון מקבל טקסט מפוענח ללא התחברות. שבורה כרגע משום שהטבלה שונתה, **אך שבור אינו סגור.**

`get_all_phones()`

**אינה**

`SECURITY DEFINER`

ולכן כפופה ל־

`RLS`

של הקורא. על

`users_total`

קיימת מדיניות אחת בלבד, ל־

`postgres`

. אינה חשופה ל־

`anon`

`get_all_phones_and_talks()`

**החשיפה הפעילה היחידה שנמצאה.**

`SECURITY DEFINER`

, פתוחה ל־

`anon`

, מחזירה את כל

`users_total`

— טלפון ו־

`linked_talk`

— ללא כל תנאי.

**נסגר 31.7.2026 ערב.** גוף הפונקציה גובה במלואו, שמה שונה ל־

`get_all_phones_and_talks_old_to_delete`

ואז בוטלה הרשאת ההרצה מ־

`anon`

,

`authenticated`

ו־

`public`

ה־

`ACL`

כולל כעת

`postgres`

ו־

`service_role`

בלבד. ה־

`Viewer`

אומת תקין אחרי הפעולה.

**לקח מרכזי: שינוי השם לבדו לא הספיק.** ההרשאה

`anon=X`

נסעה לשם החדש, כלומר החשיפה הייתה חוזרת ברגע שמישהו מחזיר את השם.

`revoke`

הוא מה שסגר. **הדפוס הזה חל על כל אובייקט נגיש ל־**

`anon`

**שנותר.**

סריקת קבצים שרצה אחרי הפעולה אישרה אפס אתרי קריאה — לפי מחרוזת, לפי משתני סביבה,
לפי שם

`rpc`

דינמי, לפי בניית כתובת בזמן ריצה, ולפי חמש סריקות רסיסים. לפונקציה אין הגדרה בשום
מקום בעץ הקבצים: היא נוצרה ישירות במסד.

### 15.8 RLS אינו מגן מפני SECURITY DEFINER

בשלוש הטבלאות שנבדקו

`relforcerowsecurity`

הוא

`false`

בעל הטבלה עוקף

`RLS`

, ופונקציית

`SECURITY DEFINER`

רצה בהרשאות הבעלים שלה. כלומר **הפעלת**

`RLS`

**אינה חוסמת פונקציית**

`DEFINER`

**שבבעלות אותו בעלים.** יש לבדוק

`relforcerowsecurity`

ולא רק

`relrowsecurity`

### 15.9 users_information — שלוש מדיניות anon פתוחות

על הטבלה היו שלוש מדיניות ל־

`anon`

עם

`qual = true`

: שליפה, עדכון והוספה. 27 שורות נגישות לקריאה ולכתיבה עם מפתח

`anon`

וללא התחברות. הטבלה הוחלפה זה מכבר ב־

`users_information_v2`

ושמה שונה ב-31.7 — **אך שינוי שם אינו מסיר מדיניות.** המדיניות נסעו עם הטבלה ועדיין קיימות.

זה אינו סותר את הקביעה "אין מדיניות

`anon`

בשום מקום" מסעיף 15.3 — אותה בדיקה כיסתה חמש טבלאות אחרות ונכונה בתחומן.

---

## 16. רשימת משימות

**עודכן 31.7.2026 ערב.**

> **הרגרסיה נסגרה 31.7.2026 ערב.** השם
>
> `talk_read_flags`
>
> ולאמת את ה־
>
> `Viewer`
>
> ראו 15.4.

### הושלם ואומת — סשן 27.7.2026

- **מחיקת** `patient_identity_map_decrypted` — חשיפת מידע רפואי נסגרה.
- **תיקון** `start_conversation_v2` — החתימה הכפולה נמחקה, ההתנגשות נפתרה.
- **השבתת** `risk-scan V2` — כפילות שבורה, `disabled_manually`.
- **מנגנון הסינון לפי פסיכולוג** — אומת קיים ופעיל, 8 מדיניות.
- **בדיקת מערכת הסיכון מקצה־לקצה** — שני המסלולים אומתו חיים.
- **ניטור כשלים** — קיים ניטור מובנה של `GitHub`. הפריט נסגר.

### הושלם ואומת — סשן 30.7.2026

- **ניקוי טבלת הזהות** — 33 מתוך 39 שורות נמחקו בטרנזקציה אחת על 13 טבלאות. כל הספירות אחרי המחיקה תאמו לתחזית. אין יתומים.
- **סגירת חשיפה פעילה** — `rpc_get_users_tzvira_v2_viewer` הייתה מפענחת, ללא סינון, ונגישה ל־`anon`.
- **הוספת סינון לפונקציית הפענוח** — זהה לתנאי שבמדיניות.
- **חיבור ה־`Viewer` לפענוח בצד השרת** — פריט 7 הישן. נדחף בקומיט `f1aac5f`.
- **מחיקת החתימה הכפולה** של `upsert_users_information_v2_from_sheet`.
- **העברת הכתיבה מהגיליון לעמודות המוצפנות** — כולל תיקון דריסת שם בערך ריק.
- **אימות שאימות המייל כבר מיושם** — פריט 4 הישן. ראו סעיף 21.3.

### הושלם ואומת — סשן 31.7.2026

- **סגירת עומס `insert_conversation_v2`** — נבדק חי. הוסרו הטריגר `trg_mirror_conversation_to_v2` והפונקציה `mirror_conversation_to_v2`, ואז חתימת ה־`text` לפי חתימה מלאה. ראו 5.1.
- **השבתת משימת תזמון 38** — שרשרת `V1` שרצה כל 15 דקות אל `conversations_prod`. הענף השני של אותה שרשרת אומת כרדום. ראו סעיף 7.
- **סקירת שלוש הפונקציות הנגישות ל־`anon`** — פריט 3 הישן נסגר. ראו 15.7.
- **שינוי שם לשבע טבלאות ישנות** לסיומת `_old_to_delete`, בשינוי שם ולא במחיקה. ראו 4.6.

### עדיפות עליונה — נותר

1. **`decrypt_user_text`** — עדיין `SECURITY DEFINER` ופתוחה ל־`anon`. שבורה כרגע, אך שבור אינו סגור. הדפוס שעבד על `get_all_phones_and_talks` חל ישירות: גיבוי גוף, שינוי שם, ואז `revoke`. ראו 15.7.
2. **סיסמת המסד** נשמרה בטקסט גלוי ב־`.claude\settings.local.json`. אומת שלא הגיעה ל־`GitHub`, אך יש להחליף אותה.
3. **שלוש מדיניות `anon` פתוחות** על `users_information_old_to_delete`. ראו 15.9.
4. **מרווח התזמון של שרשרת `V2` אינו ידוע** — קובע את ההשהיה בין סוף שיחה להופעת סיכון בפני מטפל. ראו סעיף 10.
5. **להשלים את מחיקת העמודות הגלויות מ־`patient_identity_map`** — ראו סעיף 21.2. שני חוסמים מזוהים.
6. לנקות את בלוק ה־`schedule` המת מ־`risk-scan-v2.yml`, ולהכריע אם `scripts/risk-scan.mjs` נשאר. הוא מעולם לא כתב ולו שורה אחת.
7. **`Landbot` שולח מזהה שיחה ישן אחרי כניסה חוזרת** — מקור 76 רשומות הפעלה יתומות. התיקון בזרימת `Landbot`. ראו סעיף 9.
8. **למחוק `force_end_conversation_v2(p_suffix text)`** — אין לו קורא. ולהסיר את כלי הסיום המאולץ כולו לפני ייצור.

**נסגר 31.7 ערב — `get_all_phones_and_talks`.** ראו 15.7. שינוי שם ואחריו `revoke`.

### אובייקטים שלא סווגו מעולם — לזהות לפני כל ניקוי

`ai_runs`

`landbot_runs_log`

`risk_scan_state`

`supa_run_log`

`supa_daily_migration_call_log`

(0 שורות)

`תקציר_שיעורי_הקורס_לבוט`

**זוהה 31.7 ערב:**

`ai_jobs`

הוסרה מהרשימה. הפונקציה

`openai-completion`

היא הקוראת והכותבת היחידה שלה: מושכת שורות בסטטוס

`NEW`

שולחת ל־

`OpenAI`

ומעדכנת

`answer`

וסטטוס.

מבטים שלא נסרקו:

`risk_reviews_view`

(נשבר עם הטבלה ששמה שונה),

`users_total_api`

,

`v_cron_job_run_details_il`

,

`v_supa_guarded_run_log_il`

`supa_guarded_run_log`

— כ-32,374 שורות, 4.4

`MB`

, האובייקט הגדול במסד. ללא

`RLS`

וללא מדיניות. הגידול אמור להיפסק עם השבתת משימה 38 — לאמת, ואז להחליט על שימור.

### חוסמי ייצור

4. לסגור את השער הזמני `users_viewer_risk_v2.html`.
5. `talk_read_flags_v2` — **אין לה צרכן בשום שכבה**: לא במסד, לא בפונקציות `Edge`, ואף לא אזכור אחד בקבצי הפרויקט בשליפה, בכתיבה או בסינון. מועמדת לשינוי שם, ואיתו נופלות גם שתי המדיניות הפתוחות. הנקודה העיוורת היחידה שנותרה היא `Landbot`.
6. ~~להכריע האם ה־`Viewer` צריך לקרוא `talk_read_flags` או את גרסת `_v2`~~ — **הוכרע 31.7 ערב.** הוא קורא את הטבלה ללא הסיומת, וזה הנכון. אם בעתיד יוחלט לאחד ולקרוא לטבלה החיה `_v2`, המחיר הוא עריכה ופריסה של **שלושה** קובצי צפייה, והשם `_v2` תפוס.
7. **לבדוק אם `.range()` חל על קריאת `rpc`** בגרסת הספרייה שבשימוש. קריטי לפני מטופלים אמיתיים.
8. ~~לסקור את שלוש הפונקציות מסעיף 15.7~~ — **בוצע 31.7.2026.** ראו 15.7.
9. **לסגור את נתיב העקיפה בשורה 111 של `supa-sync`** — ברירת המחדל `users_information` ללא סיומת. **אומת רדום 31.7 ערב:** שתי קריאות חיות החזירו 200, כלומר הקוראים מעבירים `table` מפורש ומגיעים לענף ה־`RPC`. עדיין ראוי להסרה.
10. **להסיר טלפון ושם מכל מקום שאינו הפסיכולוג המטפל.** מפקד החשיפה המלא — סעיף 29 ב־`SESSION_HANDOFF.md`. תוכנית ההסרה עם מספרי שורות — סעיף 30 שם.

**מבוטל:** פריט "להחזיר אימות במייל ולסגור את הכניסה העוקפת". אימות המייל **כבר מיושם**, והמילה `bypass` אינה קיימת באף קובץ `HTML` במאגר. ראו סעיף 21.3.

### BOT MONITOR

10. להוסיף ב־`BOT MONITOR` את שם הפסיכולוג המטפל.
11. להוסיף באירוע `Conversation Ended` את זמן הסיום. **קשור לפער מבני:** אין עמודת `ended_at`.

### תקלות פעילות

12. משימת תזמון 24 — תאימות `pg_net`.
13. משימות תזמון 1 ו־11 — למחוק, היעד אינו קיים.
14. לוודא שקובץ ההתראות מדווח כשל במקום להידרדר בשקט.
15. **`set_disclaimed_agreed` שבורה בשקט** — משווה מול הטלפון הממוסך, ולכן לעולם אינה מתאימה. הגרסה `_v2` תקינה. להכריע: למחוק את הישנה או לתקנה מול `phone_hash`.

**מבוטל:** הפריט "לוודא ש־`supa-sync` שולחת תמיד את מספר הפרמטרים המלא". אומת שכן.

### כפילויות

16. ~~משימות תזמון 38 ו־40 — שתיהן מצליחות. לברר מה כל אחת כותבת.~~ **מבוטל — אומת 1.8.2026.** משימה 38 מסומנת `active = false` ב־`cron.job`. רק משימה 40 רצה.
17. `Process users_total` הישן מול גרסת `V2` — שניהם פעילים.
18. שני מנגנוני הזנת תור — לברר אם רשת הביטחון עדיין נחוצה.
19. **`upsert_psychologist_from_sheet`** — ארבע מול שש חתימות. הכפילות היחידה שנותרה בסנכרון.

### השלמת ההסבה

20. למחוק `name` ו־`phone` מ־`risk_reviews_v2` — **רק אחרי** תיקון פריט 1. **אזהרה:** שורות 559 ו־987 ב־`Viewer` עדיין שולפות אותן, ומחיקה תחזיר `400` ותפיל את כל שרשרת הטעינה. **קודם להסיר מרשימת ה־`select`, ורק אז למחוק.**
21. **לתקן את `get_last_users_thread_v2`** לפענוח `name_enc` — חוסם את מחיקת העמודות הגלויות.
22. **להכריע לגבי `conversation_events_v2_view`** — קוראת את הטלפון הגלוי ומזינה את מסך המוניטור.
23. **לתקן את שלוש שורות הזהות שאינן ניתנות למחיקה** — אם ההופעה שלהן ב־`Viewer` מפריעה, הפתרון היחיד הוא עמודת סימון. מחיקה חסומה לצמיתות.

### ניקוי

24. תשע טבלאות ישנות.
25. שבעה תהליכי `GitHub Actions` ישנים.
26. `risk_engine` — להשבית או למחוק.
27. `users_viewer_risk_prod_v2.html` — עותק מיותר. **מעודכן:** שני קובצי `users_viewer_risk_prod*.html` שולפים `name, phone` מ־`users_tzvira` ללא סיומת, לפי טלפון.
28. שלושה קובצי `HTML` יתומים.
29. לאחד את מפתח ה־`anon`.
30. לתקן שמות משימות תזמון מטעים.
31. **להסיר את הקבוע `IDENTITY_TABLE`** משורה 288 — אינו בשימוש.
32. **שתי עמודות המייל ריקות לחלוטין** — `email` ו־`email_enc`. השורה היחידה שנשאה מייל נמחקה. מועמדות למחיקה מבנית.

### בירורים פתוחים

33. לסקור את הסכמה `admin`.
34. ארבע פונקציות `Edge` ישנות — האם עדיין נדרשות.
35. תפקיד `timestampz` מול `created_at`.
36. תוכן העמודה `text` ב־`psychologists_v2`.
37. מעמד `linked_talk` — מתרוקן במכוון או נטוש.
38. משמעות הערכים `A` ו־`B`.
39. מקור עדכון סיכום שלא נרשם ביומן.
40. האם `landbot_runs_log` שייכת לתכונה שפורקה.
41. **מי קורא ל־`get_last_users_thread_v2`** — אפס קוראים במאגר. ייתכן שהקורא הוא הקנבס של `Landbot`, ואז אין לו עקבה בקוד.

### שיפורים

42. להשלים כיסוי יומן ביקורת למערכת הסיכון.
43. להכריע לגבי `current_stage` — לממש או להסיר.
44. סינון עמודות ב־`Apps Script`.
45. תמיכה בגרסאות בטבלת הפרומפטים.
46. תרחיש בדיקה עם שלושת מצבי הסיכון. **הערה:** נותרו שני מטופלים בלבד עם ממצאי סיכון — דינה ו־חיים.
47. לשדרג את `Supabase CLI` מגרסה 2.65.5.

### תחזוקת Git

48. קבצי ליבה אינם תחת מעקב. כולל `CLAUDE.md`, כלי ההרצה, והפרומפטים.
49. **`docs/SESSION_LOG.md` ו־`tools/log_append.ps1` אינם תחת מעקב** — ההגנה מפני אבדן קיימת רק על הדיסק המקומי. דורש הכרעה.
50. להרחיב את `.gitignore` לתוצרי הרצה וגיבויים.

---

## 17. כללי עבודה

### מול המשתמש

צעד אחד בכל תור. הוראה, ביצוע, תוצאה, המשך.

כל פקודה מתויגת בסביבת הרצה:

`Supabase SQL Editor`

`PowerShell`

`VS Code`

הוראות ל־

`Claude Code`

תמיד בבלוק קוד.

`בדוק` = לאמת בכל האמצעים, כולל

`VS Code`

מחזור

`Git`

מלא תמיד: הוספה, שמירה, דחיפה, אימות.

### כתיבה בעברית

מילה באנגלית בתוך משפט עברי — בשורה נפרדת, עם שורה ריקה לפניה ואחריה.

אין לשבץ אותיות לטיניות בתוך מילים עבריות.

### הנדסה

**תוספת לפני מחיקה.** כל שינוי מבני הוא תוספת. מחיקה היא שלב נפרד.

**שאילתת בדיקה לפני מחיקה.** תמיד.

**השבתה לפני מחיקה** לתהליכים.

**אין עותקים ממוספרים של קבצים.** הסיומות

`_v2`

`_safe`

`_final`

אסורות על קבצים. מותרות על טבלאות במסד.

**נתוני בדיקה בערכים מזויפים בלבד.** מזהים אמיתיים עלולים לדרוס שורות חיות.

### מגבלות ידועות

`Supabase SQL Editor`

אינו מריץ בלוק המשלב

`DDL`

עם גוף פונקציה בציטוט דולר. יש להפריד.

`BEGIN`

ו־

`COMMIT`

חייבים לרוץ באותו בלוק ביצוע.

`Claude Code`

רואה את עץ הקבצים בלבד. אין לו ראייה לסכמה החיה. יש להצליב בשאילתה ישירה.

הודעת

`Docker is not running`

בעת פריסה אינה כשל. סימן ההצלחה:

`Deployed Functions on project`

ב־

`PowerShell`

מחרוזות המכילות

`@`

עלולות להשתבש. יש לתקן מיד בתיקון ההודעה לפני הדחיפה.

מיסוך: העמודה

`phone`

מכילה ערכים ממוסכים. השוואה למספר מלא תחזיר אפס שורות בשקט. יש להשתמש בתבנית סיומת.

---

## 18. מה לא אומת

- שמות בלוקי הזרימה בחשבון `Landbot`
- משמעות הערכים `A` ו־`B`
- תוכן הסכמה `admin`
- תפקידן של ארבע פונקציות ה־`Edge` הישנות
- מבנה `risk_phrases`
- **`talk_read_flags_v2` אומת 31.7 ערב:** `time_key`, `patient_code`, `is_hidden`, `toggled_by`, `patient_name`, `created_at`, `updated_at`. 74 שורות, כולן עם `patient_name` מאוכלס — ואיש אינו קורא אותן
- האם הבקשה במשימת תזמון 24 נשלחת למרות הכשל באיסוף
- האם עמודות `risk_reviews_v2` שהקוד כותב אליהן תואמות בכל מסלול

---

## 19. סביבה

**מאגר:** `yaki-tamir-levy/landbot-runner`, ענף `main`

**פרויקט Supabase:** `qcwimczsiuxkarwfiyai`

**תיקייה מקומית:** `C:\Users\User\Documents\landbot-runner-clean`

**סביבת כוונון:** `C:\Users\User\Documents\landbot-runner-clean\bot simulation`

**תיקיית דוחות:** `C:\Users\User\Documents\landbot-runner-clean\powershell_text`

כל דוח מ־

`Claude Code`

נכתב אל

`POWERSHELL_TXT.txt`

בתיקייה זו, באנגלית, בקידוד

`UTF-8`

, במצב דריסה.

**כלים:** `VS Code` עם `Claude Code`, `PowerShell`, `Supabase SQL Editor`

הכלי

`gh`

אינו זמין במסוף

`VS Code`

— יש להשתמש ב־

`PowerShell`

---

## 20. סביבת הכוונון — קבוצה ג'

**מנותקת מייצור.** אינה נוגעת בנתוני מטופלים.

מיקום:

`bot simulation`

### מחזור הכוונון

מטפל ← שופט ראשון ← שופט שני ← מתקן ← עדכון פרומפט ← מחזור הבא

הבחירה בין השופטים היא **הציון השמרני**.

### כלי ההרצה — שמות קבועים

`run_iterative_bot_tuning.ps1`

— מנגנון הכוונון האיטרטיבי. מריץ שיחות בדיקה, מקבל ציונים, מפעיל מתקן, מציע שיפור לפרומפט, ושומר את הטוב ביותר עד כה.

`run_runtime_corrector_replay.ps1`

— מריץ שיחה קיימת דרך מועמד ומתקן.

**אסור** ליצור גרסאות בשמות

`_v2`

`_safe`

`_logged`

`_final`

### קובצי הפלט של ההרצה החוזרת

`original_candidates.rtf`

— כל תשובות המועמד המקוריות

`corrected_conversation.rtf`

— השיחה לאחר החלטות המתקן

`runtime_corrector_trace.jsonl`

— תיעוד מכונה של כל צעד, החלטה וקוד סיבה

### תיקיית הפרומפטים

`tuning_prompts`

מפרידה את ההוראות מקוד

`PowerShell`

, כך שניתן לשנות את תוכן ההערכה בלי לגעת במנגנון ההרצה. עשרה קבצים:

`judge_system_prompt.rtf`

`judge_user_template.rtf`

`corrector_system_prompt.rtf`

`corrector_user_template.rtf`

`corrector_retry_template.rtf`

`judge_rubric_general.rtf`

`judge_rubric_four_question_regression.rtf`

`json_retry_system_prompt.rtf`

`json_retry_user_template.rtf`

`runtime_corrector_system_prompt.rtf`

### תיקיות התוצאות

`tuning_runs\<RUN_ID>\cycle_XX`

ובה:

`prompt_diff_cycle_N.txt`

`prompt_change_summary_cycle_N.txt`

`best_so_far_decision_cycle_N.txt`

`conversations\<RUN_ID>`

— למניעת ערבוב תוצאות בין הרצות.

### כללים

קובצי

`RTF`

אמיתיים בלבד, עם

`Unicode`

תקין.

**סימולציות אינן משנות ייצור.** הפרדה מלאה.

---

## 21. סשן 30.7.2026 — ניקוי הזהויות והשלמת ההצפנה

### 21.1 ניקוי טבלת הזהות

**המצב לפני:** 39 שורות. התיעוד קבע 38 בכל מקום — הפרש של אחת שמעולם לא הובחן.

**שש מהשורות לא היו מטופלים אלא פרומפטים.** זוהו לפי הטלפון הממוסך, שתאם אחד לאחד את המזהים הקודמים שבסעיף 13:

`999***999`

,

`999***990`

,

`888***888`

,

`777***777`

,

`666***666`

,

`123***789`

כלומר סנכרון מהגיליון ניתב בעבר שורות פרומפט אל

`get_or_create_patient_code`

, וזו יצרה להן זהויות אמיתיות. **הן לא יחזרו** — הפרומפטים עברו לגיליון נפרד שאין בו עמודת טלפון.

**נשמרו שלוש זהויות:** דינה, חיים, משה. הבחירה נשענה על כיסוי בדיקה: דינה נושאת את שבעת ממצאי הסיכון ואת שני מסלולי הסריקה; חיים מספק דוגמה שנייה; משה עבר במערכת בגרסת הקוד הנוכחית.

**שלוש זהויות נוספות אינן ניתנות למחיקה כלל.** יש להן רשומות ב־

`conversation_events_v2`

, והמחיקה משם חסומה בטריגר. הבת חסומה, ולכן האם חסומה. **זהו הגבול המעשי של "התחלה על נקי".**

**המחיקה עצמה:** טרנזקציה אחת, 13 טבלאות, בסדר הנגזר ממפות המפתחות הזרים.

`conversation_events_v2`

**הוחרגה במכוון** — לכל 33 השורות היו אפס אירועים, ולכן לא היה מה למחוק, ונגיעה בטבלה הייתה מעירה את הטריגר לחינם.

**אומת: אין ולו מפתח זר אחד עם**

`ON DELETE CASCADE`

**בסביבת הזהות.** כל שבעת המפתחות הם

`NO ACTION`

, כלומר מחיקה נכשלת כשקיימות שורות בת — ולא גוררת מחיקה שקטה.

**שבע טבלאות נוספות מחזיקות**

`patient_code`

**ללא מפתח זר**, ולכן אינן חוסמות ומשאירות יתומים בשקט. נכללו במחיקה במפורש.

**אימות אחרי:** שש הספירות תאמו במדויק לתחזית שנגזרה מהסקירה לפי שורה. **אין יתומים.**

### 21.2 השלמת מסלול ההצפנה

**הבעיה שנסגרה:** ההצפנה נבנתה כתוספת, ושלב ההחלפה מעולם לא בוצע. הטבלה החזיקה שני עמודים במקביל — מוצפן וגלוי — וכל המערכת קראה וכתבה לגלוי.

**מה שנעשה, בסדר הזה:**

שלילת הרשאת ההרצה מפונקציית הפענוח, הוספת סינון לגופה, החזרת ההרשאה ל־

`authenticated`

בלבד, הפניית ה־

`Viewer`

אליה, מחיקת החתימה הכפולה בסנכרון, והעברת הכתיבה לעמודות המוצפנות.

**אומת חי:** שינוי שם בגיליון מתפשט לאורך כל השרשרת ומופיע ב־

`Viewer`

**מה שנותר לפני מחיקת העמודות הגלויות — שני חוסמים מזוהים:**

`get_last_users_thread_v2`

מצטרפת נכון לפי

`phone_hash`

, אך שולפת

`pim.name`

ו־

`coalesce(ui.phone, pim.phone)`

מבנה הפלט מתאים למה ש־

`Landbot`

צריך בפתיחת שיחה, ולכן **סביר שזו פונקציית ייצור** — סביר, לא מאומת. **התיקון אינו דורש לדעת מי קורא לה:** די להפוך אותה לפענוח

`name_enc`

, והחוסם נעלם.

`conversation_events_v2_view`

מכילה

`pim.phone AS phone_masked`

, וממנה ניזון מסך המוניטור. מחיקת הטלפון תשבור אותו.

**שלוש פונקציות אינן חוסמות** — מצטרפות לפי

`phone_hash`

בלבד:

`get_summarized_linked_talk_v2`

,

`set_disclaimed_agreed_v2`

,

`trace_patient_flow_v2`

**העמודה**

`email`

**פנויה למחיקה** — אין לה צרכן, והיא ריקה לחלוטין.

### 21.3 אימות המייל — כבר מיושם

**מבוטל:** הפריט "להחזיר אימות במייל ולסגור את הכניסה העוקפת".

**השרשרת המאומתת:** הדפדפן מטפל בטלפון בלבד. הוא שולח

`{ phone }`

אל

`start-login`

, ו־

`{ phone, token }`

אל

`verify-login`

הפונקציה

`verify-login`

שולפת את המייל מ־

`psychologists_v2`

לפי הטלפון, דוחה מייל ריק או משתמש שאינו פעיל, ומריצה

`verifyOtp`

עם

`type: "email"`

הדפדפן קורא רק ל־

`setSession`

ול־

`getSession`

— לא ל־

`signInWithOtp`

ולא ל־

`signInWithPassword`

**זהות האימות היא מייל. הטלפון הוא מפתח חיפוש בלבד.**

**המילה**

`bypass`

**אינה מופיעה באף קובץ**

`HTML`

**במאגר.** הדבר היחיד שנראה כך הוא

`localStorage.setItem('otp_verified','1')`

— **ואף קובץ אינו קורא את המפתח הזה.** הוא אינו חוסם ואינו מאפשר דבר. השער בפועל הוא

`supa.auth.getSession()`

בשורה 302, הבודק קיום הפעלה בלבד ולא תביעה כלשהי.

**לא אומת:** מערך התביעות המדויק באסימון חי. הוסק ממסלול ההנפקה, לא מפענוח אסימון.

---

## 22. לקחים — כשלים ושגיאות שאין לחזור עליהם

### 22.1 היעדר סינון חמור מהיעדר הצפנה

הפונקציה המפענחת נבנתה כדי **להגן**, והייתה החשיפה הגדולה ביותר שנמצאה: היא עקפה את שער האימות ואת כל שמונה המדיניות, משום שהייתה

`SECURITY DEFINER`

בלי תנאי סינון.

**הכלל:** פונקציה

`SECURITY DEFINER`

חייבת לשאת את תנאי הסינון **בגופה**. אין להישען על הרשאות, ואין להניח שמי שקורא לה כבר סוננה.

### 22.2 הרשאה אינה כוונה

כמעט כל פונקציה בסכמה פתוחה ל־

`anon`

, משום שזו ברירת המחדל של

`PostgreSQL`

**אין להסיק מהרשאה שפונקציה נועדה לשימוש חיצוני, ואין להסיק שמישהו "פתח" אותה.** ניסיון להשתמש בהרשאות כמדד לזיהוי קוראים נכשל בסשן הזה.

### 22.3 סקירת מאגר אינה סקירת מערכת

הסקירה שקבעה "צרכן אחד בלבד" הייתה נכונה — **ופספסה שני צרכנים במסד**: פונקציה ו־

`view`

זהו אותו כשל שכבר תועד, כשמחיקת עמודה אומתה מול ה־

`Viewer`

בלבד והרגה סקריפט ליום.

**הכלל:** לפני כל מחיקת עמודה — לסרוק **גם** את

`pg_proc`

**וגם** את

`information_schema.views`

, ולא רק את עץ הקבצים.

### 22.4 אין להסיק התנהגות מסיכום

שלוש טעויות בסשן הזה נבעו מאותו מקור: הסתמכות על **סיכום** שהופק על ידי כלי, במקום קריאת הקוד עצמו.

הטענה ש־

`data-name`

ניזון מ־

`risk_reviews_v2`

— נשענה על סיכום ולא על בדיקת מהו המשתנה.

הטענה על "מטופל בלתי נראה" — נשענה על דיווח שתאים ריקים מושמטים, בלי לקרוא את לוגיקת הדילוג.

הטענה שהפרומפטים יחזרו — נשענה על היעדר סינון במסד, בלי לדעת שהגיליון שונה.

**הכלל:** כשמדובר בהתנהגות שממנה נגזרת פעולה — לקרוא את המקור הגולמי, לא דוח.

### 22.5 מיסוך שנשכח שובר השוואות בשקט

`set_disclaimed_agreed`

משווה טלפון מלא מול העמודה הממוסכת. שש ספרות מול עשר. **הפונקציה רצה, אינה נכשלת, ואינה מעדכנת דבר.**

זו אותה נקודת כשל שכבר מתועדת — והיא התממשה בפועל בלי שאיש ידע.

### 22.6 מחיקה אינה השלמה של תוספת

ההצפנה נוספה, והשלב השני — הפניית הצרכנים ומחיקת הישן — לא בוצע. התוצאה: מערכת שנראית מוצפנת ופועלת גלוי.

אותו דפוס בדיוק חוזר בעמודות הצל של

`risk_reviews_v2`

**הכלל:** "תוספת לפני מחיקה" אינו רישיון לדלג על המחיקה. שלב ההחלפה הוא חלק מהמשימה, לא המשך אופציונלי.

### 22.7 הסרת כותב אינה זהה למחיקתו

התוכנית להסיר את הכתיבה הגלויה מפונקציית הסנכרון הייתה שוברת את עדכון השמות, משום ש־

`get_or_create_patient_code`

מצפינה **רק ביצירה**. שינוי שם למטופל קיים היה מפסיק להגיע לשום מקום.

**הכלל:** לפני הסרת כתיבה — לוודא שקיים מסלול חלופי שכותב בפועל, ולא רק שהעמודה קיימת.

### 22.8 ה־Viewer המקומי אינו מה שרואים

עריכה שלא נדחפה אינה קיימת עבור

`GitHub Pages`

בדיקה בדפדפן לפני

`push`

בודקת את הקובץ הישן — כלומר אינה בודקת דבר.

**הכלל:** להשוות את חתימת הגרסה בכותרת אל ה־

`SHA`

שנדחף, לפני כל מסקנה.

### 22.9 הממשק מזריק limit לשאילתות

`Supabase SQL Editor`

מוסיף

`limit 100`

אוטומטית, ולעיתים בתוך רשימת

`IN`

— מה שמייצר שגיאת תחביר שאינה בשאילתה. הפתרון:

`No limit`

, או פיצול לשאילתות ללא

`IN`

בנוסף:

`pg_get_functiondef()`

נכשלת על פונקציות צבירה. יש לסנן

`prokind in ('f','p')`

לפני הקריאה.

### 22.10 תיעוד שסותר את המסד מוביל לאבחון שגוי

האבחון הראשון בסשן היה שגוי משום שנשען על סעיף במדריך שקבע קיום מדיניות זמנית פתוחה — מדיניות שכבר לא הייתה קיימת.

**הכלל:** תיעוד הוא נקודת פתיחה, לא ראיה. כל טענה שממנה נגזרת פעולה — לאמת מול המסד באותו תור.
