# מצב מזוקק ומשימות פתוחות

**עודכן: 17 בספטמבר 2026** — סשן הצלבת התיעוד מול המסד החי, אחרי חמישה סשנים שלא זוקקו לכאן. מחליף את הגרסה הקודמת במלואה.

**כלל:** כל פריט כאן מסומן

`מאומת`

או

`עדות`.

פריט שאינו מאומת אינו בסיס לפעולה.

---

## 0. שינוי מצב — לקריאה ראשונה

**מסלול הקבלה מחלץ מגדר.** פריט 47 נסגר ב-17.9, ואומת מקצה לקצה: מועמד
שדיבר בלשון נקבה נשמר כ-

`F`

והפך למטופל עם אותו ערך. **שרשרת המגדר שלמה מהגיליון ומהקבלה ועד המודל.**

**תהליך ההכרעה היה מושבת** ומועמדים נשארו ללא הכרעה. הופעל מחדש, והתזמון
שונה לכל שעה עגולה בין 08:00 ל-00:00 שעון ישראל. **מי השבית ומתי — לא
מתועד.**

**נוסף פענוח נתוני מטופל למנהל בלבד**, עם יומן הוספה-בלבד ותקרה של עשרים
ניסיונות בשעה. ראו סעיף 13.

**חמישה תיקונים לתיעוד אומתו מול המסד ב-17.9** — ראו סעיף 13.3. הבולט
שבהם:

`get_last_users_thread_v2`

**אינה חשופה ל-**

`anon`,

בניגוד למה שנכתב כאן.

**חוזק הפענוח תלוי בפריט 27** — אין עדיין הגבלה על ניחוש קוד האימות
ב-

`start-login`

וב-

`verify-login`.

**כל 28 המטופלים הפעילים הם נתוני בדיקה.** המטופלת האמיתית היחידה
סומנה

`active = NO`

ב-16.9.

**המאגר ציבורי.** טלפון ממוסך אמיתי נתפס בקובץ מיגרציה לפני דחיפה ב-17.9.

---

## 1. חוסמי ייצור — לפי חומרה

### 1.1 `bot-monitor` חשופה — החמור ביותר

**מאומת בקריאה חיה מסביבה חיצונית, ללא כל אישור.**

אין

`x-landbot-secret`.

אין

`JWT`.

`CORS`

פתוח ב־

`*`.

הפונקציה קוראת ל־

`conversation_events_v2_view`,

שעליו

`RLS`

**כבוי** ואפס מדיניות.

**מה נחשף:** מזהי מטופלים, טלפונים ממוסכים, שמות מטפלים, מבנה השרשרת ותזמונים.

**הכרעה נדרשת:** סגירת שער בכותרת סוד תשבור את

`monitor/index.html`

ב־

`GitHub Pages`

— סוד בדף פומבי אינו סוד.

### 1.2 חשיפת המאגר הציבורי

`ad37a97`

נדחף למאגר ציבורי. 1,772 הוספות, מהן 47 מאושרות.

**קיים בהיסטוריה:** כתובת מייל אישית, ושורה על מיקום ותוקף אישור גישה למסד.

**ארבע הכרעות:** מעמד המאגר, החלפת אישור הגישה, כתיבה מחדש של ההיסטוריה, סקירת 1,512 השורות שלא נבדקו.

### 1.3 ההתראה הקיימת משדרת מידע מזוהה

`tools/risk_reviews_notify.py`

בונה גוף הודעה בתבנית

`<שם> | <טלפון> | <טקסט סיכון>`,

שורות 405–413.

ב־

`Pushover`

— עד חמש שורות, מוצג במסך הנעילה בלי לפתוח את המכשיר, עדיפות 1 וצליל

`siren`.

הקיצור ל־120 תווים קוסמטי ואינו נוגע בשם ובטלפון.

ב־**מייל** — אותם שדות, **בלי קיצור ובלי תקרת חמש שורות.**

**מעמד נוכחי:** מקובל כערוץ ניטור של בעל המערכת בלבד. **אינו מיועד להרחבה לפסיכולוגים** — לשם כך קיים מנגנון ההתראות במייל שנבנה ב-6.8.2026.

### 1.4 קוד בייצור שאינו בענף

39 שורות בגרסה 36 של

`runtime-corrected-response`

— בלוק ה־

`INSERT`

ל־

`corrector_test_log`.

נפרס, לא נשמר בקומיט. **עדות** — לא אומת.

### 1.5 שער פתוח ב־Viewer

`users_viewer_risk_v2.html`

ללא אימות, במכוון, לבדיקות.

### 1.6 הפעלה ללא הגבלת זמן

אין ניתוק לפי זמן ואין

`signOut`

יזום. האסימונים חוזרים בגוף התשובה ללא

`cookie`.

### 1.7 שני `views` בלי `RLS`, פתוחים ל-`anon`, נושאים שם וטלפון — חדש, 13.9.2026

`risk_reviews_view`

ו־

`users_total_api`.

`conversation_events_v2_view`

כבר מתועד בסעיף 1.1 כחלק מחשיפת

`bot-monitor`.

**שני האחרים אינם מתועדים במקום אחר.** שניהם

`views`,

`security_invoker`

אינו מוגדר והבעלים הוא

`postgres`,

ולכן הם רצים בהרשאות הבעלים ועוקפים את

`RLS`

לגמרי.

### 1.8 בליעת שיחות — מחזור ההעברה אוסף לפי מטופל ולא לפי שיחה

תועד ביומן הגולמי ב-14.8.2026 בפירוש כ"חוסם ייצור", ולא נסגר מאז.
פירוט המנגנון טרם זוקק לכאן — ראו

`docs/SESSION_LOG.md`,

14.8.2026, סעיף 11.

---

## 2. תקינות נתונים

### 2.1 רשומות הפעלה יתומות

**מאומת 1.8.2026:** 160 רשומות ב־

`conversations_session_v2`.

8 ללא אף אירוע. 80 שיחות עם אירוע יחיד.

**הסיבה, עדות:**

`insert_conversation_v2`

אינה ממציאה מזהה שיחה. מזהה ישן שנשלח מ־

`Landbot`

מתקבל ללא שגיאה. **התיקון ב־**

`Landbot`.

### 2.2 מטופל בלי שיוך — **סגור 11.8.2026**

מטופל אחד נשא ממצאי סיכון ולא הייתה לו שורה ב־

`users_information_v2`.

**זוהה:** חיים,

`054***798`.

**נמחק ב-11.8.2026** יחד עם משה,

`222***223`.

הפריט סגור בהיעדר הנתון, לא בתיקון המנגנון.

**מה נשאר פתוח מבחינה מבנית:** שרשרת ההיתר ב־

`RLS`

נשענת על התאמה, ולכן מטופל חדש בלי שיוך ייעלם מכל פסיכולוג ב־

`Viewer`

שוב. מנגנון ההתראות מנתב פריטים כאלה לחשבון האדמין, בנוסח נפרד המורה לשייך מטפל — הניתוב פותר את ההתראה, לא את הצפייה.

### 2.3 משתמש ללא רשומה מקבילה

משתמש רביעי ב־

`auth.users`

שאינו קיים ב־

`psychologists_v2`.

יעבור את שער האימות ויקבל מסך ריק.

### 2.4 סמיכות טלפונים

שני טלפוני מטפלים נבדלים בספרה אחרונה אחת, והאחד הוא חשבון המנהל. הטלפון הוא מפתח החיפוש בכניסה.

### 2.5 אפס מוביל בטלפון המטפל

`start-login`

דורשת

`^05\d{8}$`.

**מאומת:** שלוש השורות הקיימות תואמות.

**הסיכון:** שורה שתיכנס מהגיליון בלי אפס מוביל תיכשל בשקט — המשתמש יראה הודעת הצלחה ולא יקבל קוד.

---

## 3. תיקוני תיעוד — מאומתים, דורשים עדכון ב־PROJECT_GUIDE

| סעיף | הטענה במדריך | מאומת 6.8.2026 |
|---|---|---|
| 10 | הלינק ב־`Pushover` מוביל לשיחה | **סטטי**, מהסוד `PUSHOVER_URL`, ללא מזהה מטופל |
| 10 | `Pushover` נשלח מ־`pushover_notify.yml` בלבד | פועלי התור משגרים אותו גם ב־`workflow_dispatch` |
| 10 | ניטור כשלים מכוסה | שיגור `Pushover` שנכשל **נבלע ב־`catch`** |
| 8 | שני תהליכי גיבוי אינם רצים | שניהם `yml` תקין עם `cron` פעיל ארבע פעמים ביום |
| 11 | אין ערוץ מייל לפסיכולוגים | קיים מ־6.8.2026 |

**הפרכה:** שם הקובץ

`process-queue-worker-v2.before_pushover.psbackup.yml`

מטעה. ההפרש הוא **שורה אחת** — הזרקת

`GH_WORKFLOW_TOKEN`.

ה־

`Pushover`

חי ב־

`scripts/process_queue_worker_v2.mjs`.

**שחזור הגיבוי כתהליך פעיל יפיל את הפועל כולו** — הסקריפט קורא את המשתנה בטעינת המודול.

### הערה תפעולית קבועה — מסלול הפרומפטים

הבחירה בין מסלולי הפרומפט יושבת ב־

`get_prompt_v2`

במסד. הקנבס מעביר מסלול בלבד ואינו מכיר את שמות הפרומפטים עצמם.

**מוסכם במפורש: הקורקטור משותף לשני המסלולים.**

`runtime-corrected-response`

שולפת את השורה

`corrector`

בשאילתה קשיחה, ואינה עוברת דרך

`get_prompt_v2`.

זהו החריג היחיד, והוא בהחלטה.

כללי עריכת פרומפט מכאן והלאה: מסלול

`NLP_CBT`

— לערוך בשורות

`nlp_`;

מסלול

`CLINIC`

— לערוך בשורות

`clinic_`;

הקורקטור — לערוך בשורה

`corrector`

בלבד. שלוש השורות המקוריות

`therapist`

ו־

`pre_patient`

אינן נקראות עוד. אין למחוק אותן.

---

## 4. משימות פתוחות

### מיידי

1. לסגור את החשיפה של `bot-monitor`. **חוסם.**
2. להכריע בארבע ההכרעות של המאגר הציבורי. **חוסם.**
3. לקמט את 39 השורות של גרסה 36 ולדחוף.
4. לסגור את `users_viewer_risk_v2.html`.
5. **לבדוק בממשק** `Actions` **את מצב ההפעלה של שני קובצי הגיבוי** `pushover_notify.*.psbackup.yml`. אם פעילים — להשבית.

### מנגנון ההתראות החדש — מעקב

6. לוודא שההרצה המתוזמנת ב־20:00 שעון ישראל פועלת בפועל, בפעם הראשונה.
7. לוודא שההרצה השעתית ב־`:30` תופסת ממצא סיכון חדש.
8. **הנחיית הצטרפות לפסיכולוג:** לסמן את המייל כ`אינו ספאם` ולהוסיף את השולח לאנשי קשר. אומת שההודעה נוחתת בקטגוריה משנית ב־`Gmail`.
9. סטטוסי "פתוח" הם רשימה קשיחה בקוד: `NEW`, `REVIEWED`, `VIEWED`. סטטוס חדש לא ייספר. **מאומת 11.8.2026:** הערכים בפועל ב־`risk_reviews_v2` הם `CHECKED` ו־`NEW` בלבד — `VIEWED` ו־`REVIEWED` אינם קיימים באף שורה, אך מופיעים ברשימה הקשיחה בקוד. הוכרע: `NEW` לא טופל, `CHECKED` טופל; התוצאה זהה לרשימה הקיימת כל עוד `VIEWED`/`REVIEWED` אינם נכתבים בפועל. (בנפרד: `status` ו־`therapy_track` הם שני צירים בלתי תלויים — `status` מתאר הרשאה, `therapy_track` מתאר מסלול טיפול. אין לגזור סוג שיחה מ־`status`.)
10. סימן המים משווה מחרוזות, כי `time_key` היא `text`. **שינוי פורמט ישבור את ההשוואה בשקט.**

### משוחרר

11. למחוק `name` ו־`phone` מ־`risk_reviews_v2`. **קודם** להסיר משורות 559 ו־987 ב־`Viewer`.
12. **נסגר 13.9.2026.** הפונקציה מפענחת

`name_enc`.

**החוסם למחיקת העמודות הגלויות הוסר** — ראו סעיף 8.

### ניקוי

13. למחוק `force_end_conversation_v2(p_suffix text)` — אין קורא.
14. להסיר את כלי הסיום המאולץ לפני ייצור.
16. `set_disclaimed_agreed(p_phone text)` — משווה מול טלפון ממוסך, לעולם אינה מתאימה.
17. כפילות מספרי גרסה בכותרת המוניטור.
18. `IDENTITY_TABLE` בשורה 288 ב־`Viewer` — אינו בשימוש.
19. הסרה סופית של `risk-scan V2`, כולל בלוק ה־`schedule`.

### בירורים

20. **נסגר 13.9.2026.** שבע הפונקציות שרצו בייצור ללא מקור בגיט גובו
מהשרת ונדחפו: `meitar-chat-proxy`, `meitar-lookup-proxy`,
`meitar-otp-gate`, `sim-patient-proxy`, `intake-status`, `crypto-tool`,
`secure-users-information`. קומיטים `79ad8f1` ו-`f902f59`.
**אלה עותקי גיבוי בלבד** — נמשכו מהשרת כפי שהם ולא נסקרו מול כוונה.
הם מבטיחים שיש מה לשחזר, לא שהקוד נכון.
21. שלוש פונקציות שגופן לא נקרא: `decrypt_user_text`, `get_all_phones`, `get_all_phones_and_talks`.
22. אין רישום פריסה ואין `config.toml`.
23. הגדרת הפרטיות של הבאקט `corrector-test-log`.
24. `tools/log_append.ps1` מחוץ למעקב.
25. `POWERSHELL_TXT.rtf` נדרס בכל כתיבה. שווה חותמת זמן בשם.
26. **ערכי הפריסה של** `DEBUG_ENUM` **ו־**`ALLOWED_ORIGINS` **בפונקציות ההזדהות** — לא ניתנים לקריאה מהמאגר.
27. **אין הגבלת קצב** ב־`start-login` וב־`verify-login`. **מ-17.9 זהו גם תנאי לחוזק הפענוח למנהל** — ההגנה שם טובה בדיוק כמו כניסת המנהל.

### לא לתקן כרגע — החלטה מפורשת

28. ליקויי הפרומפט שנצפו אחרי התיקון — לצבור עוד שיחות לפני שינוי נוסף.
29. **`Hook` להתראה בסלולר בסיום משימת** `Claude Code` — נדחה במפורש לסשן המשך.

### מ-8.8.2026 ואילך — מסלולי טיפול, סוג שיחה וסינון

**מקופל לכאן מהבלוקים הישנים "עדכון 8.8.2026" ו"עדכון 11.8.2026" ומהבלוק "מסלול טיפול — 7.8.2026", שהוסרו כבלוקים נפרדים — זהו קובץ מזוקק, לא יומן.**

30. **תוכן קליני חסר במסלול `CLINIC`.** `clinic_therapist` 84 תווים, `clinic_pre_patient` 92 תווים — מחזיקי מקום. **אין להעביר מטופל אמיתי למסלול `CLINIC` עד שיוזן תוכן אמיתי.**
31. **שני מטופלי בדיקה כבר במסלול `CLINIC`:** `222***222` ו־`444***444`, שניהם `active = yes`. לוודא שזה מכוון.
32. **`course_guide` איבדה תו אחד בסנכרון של 8.8.2026.** לא הוסבר. הערך שלפני קיים בשני הגיבויים.
33. **`clinic_corrector` — ליישר או להשאיר.** הושארה מיושנת במכוון ב־14.8.2026: אינה כפילות, אלא הרכיב שיאפשר למסלול `CLINIC` מתקן משלו בעתיד. פתוח מאז, לא הוכרע. (שלוש שורות אחרות מאותה קבוצה — `therapist`, `pre_patient`, `nlp_corrector` — כבר סומנו `DEAD` ב־14.8.2026; ראו סעיף 0.)
34. שורות שמגיעות מהגיליון עדיין נושאות רווחים מובילים וסוגרים. בלתי מזיק — הפונקציה מנקה בכניסה.
35. **סנכרון `therapy_track` מהגיליון.** אומת שהפונקציות אינן דורסות ערך קיים — הן מונות עמודות במפורש ב־`on conflict do update set`. **הסיכון האמיתי הפוך:** שורה חדשה עלולה לקבל ברירת מחדל בשקט. לא תוקן.
36. **החתימה `start_conversation_v2(p_patient_code uuid)` אינה כותבת `source` כלל.** להכריע אם החתימה הזו בשימוש.
37. **לאחד את שני העותקים הנותרים של רשימת סטטוסי הסיכון** ב־`users_viewer_tzvira_v2.html` — בלולאת `notRev` ובתוך `renderDialogHtml()`. שניים אחרים אוחדו כבר ל־`isRiskStatusOpen()`.
38. **להסיר `statusLabel`, `STATUS_LABEL_MAP` ו־`row._riskOverrideKind`** מ־`users_viewer_tzvira_v2.html` — קוד מת, אפס קוראים.
39. **להוסיף `powershell_text/` ל־`.gitignore`.** קובץ הדוח הוא מועמד להיסחף בכל `git add` גורף.

### ממצאים חדשים — 13.9.2026

40. **נסגר 13.9.2026.** הערך

`C`

הוא מערכת

`Meitar`

— הבוט הטיפולי אחרי ההסבה מלנדבוט לקוד. ראו סעיף 8.
41. **בדיקת שפיות על פלט מודל המטופל לפני כתיבה למסד.** מקור: 14.8.2026 — מודל המטופל בסביבת הכוונון ייצר פעם אחת מתוך כמאתיים תורים טקסט זבל בסינית וחזרה על הקלט. אין כרגע שכבת בדיקה לפני כתיבה.
42. **`summaryHeaderIsrael` מצמידה `Z` לשעון ישראל** בתצוגה. מקור: 14.8.2026 — אותו דפוס בדיוק כמו `time_key`, שתוקן באותו יום; כאן מדובר בטקסט תצוגה בלבד, ולא תוקן.
43. **נסגר 15.9.2026.** למסלול `KURS` לא היה מיפוי פרומפטים, והוא היה
    נופל בשקט להוראות `NLP_CBT`. **מצב הקורס נקבע כעת לפי השיחה, ולא
    לפי therapy_track כלל** — מפת הניתוב לפי מסלול אינה נגעת יותר בנתיב
    הקורס, ולכן `KURS` אינו יכול להגיע אליה. ראו סעיף 10.
44. **עקיפת אימות בדף מית"ר.** הסימון שקובע "המטופל כבר הזדהה" נשמר
    ב-`localStorage` ללא תפוגה, וניתן לכתיבה ביד. בנוסף, שלוש פונקציות
    התיווך אינן דורשות שום אימות, ולכן אפשר לעקוף את הדף לגמרי.
    **חוסם ייצור.** נדחה במכוון 13.9.2026 — כל המטופלים כרגע נתוני בדיקה.
    **התיקון המתוכנן:** אסימון שהשרת מנפיק, שממנו נגזרת זהות המטופל,
    במקום סימון שהלקוח כותב.
45. **קוד האימות לתיבה אחת קבועה בקוד.** **אינו חסום בנתונים:** אומת
    13.9.2026 שכל ששת המטופלים ממסלול הקבלה נושאים דוא"ל, וכל 28 חסרי
    הדוא"ל הם מהגיליון ומהסימולציה — נתוני בדיקה שיימחקו.
    **מטופל אמיתי תמיד יגיע עם דוא"ל משלו.** התיקון: לשלוף את הדוא"ל של
    המטופל במקום הקבוע, עם נפילה אחורה לקבוע.
46. **זיהוי לפי טלפון ממוסך.** שליפת ההוראות מחפשת לפי שלוש ספרות
    ראשונות ושלוש אחרונות. שני מטופלים יכולים ליפול לאותה תבנית, ואז
    המערכת נסוגה בשקט למסלול ברירת המחדל. **אין התנגשות היום — אומת.**

### ממצאים חדשים — 15.9.2026

47. **נסגר 17.9.2026.** הפרומפט מחלץ

`gender`

מהאופן שבו המועמד מדבר על עצמו. ספק — ריק. **אינו נכנס לרשימת החסרים
ואינו יכול לחסום קבלה.** אומת מקצה לקצה. ראו סעיף 13.
48. **אין הגבלה על ניחוש קוד אימות.** שישה ניסיונות שגויים עברו בלי חסימה.
    המגבלה שנבנתה ב-15.9 חלה על **שליחת** קוד בלבד: שלוש שליחות לשעה,
    נספרות ב-`otp_send_log.send_count`.
49. **מחיקת** `app_guidance`. החשיפה נסגרה ב-15.9; הטבלה ממתינה לסריקת
    תלויות **בשני עולמות** — `pg_proc` ו-`information_schema.views`, לא רק
    עץ הקבצים. מכילה 21 שורות עם שם וטלפון, וחמישה פרומפטים ישנים.
50. **הטקסט** `intake_candidate` **לשאוב מהמסד** בקובץ המועמדים, כפי שנעשה
    בדף מית"ר. הנוסח בדף ובמסד כבר זהה מילה במילה.
51. **בדיקת טלפון כפול במסלול המועמד** — לאמת מה בדיוק היא בודקת. בדף קיים
    מסך "המספר הזה כבר רשום", ולכן **סביר שהיא כבר מיושמת** — לא אומת.
52. **לשמור את סקריפט המרת ה-**`RTF` **כקובץ קבוע** תחת `tools`, במקום
    לייצר אותו מחדש בכל דוח. ראו לקח 22.22.
53. **להוסיף** `powershell_text/` **ל-**`.gitignore`. התיקייה אינה מוגנת,
    וקובץ הדוח נעלם פעם אחת ב-13.9.

- **העמודה** `recent` **ב-**`patient_ab_for_viewer` **ללא צרכן.** נוספה
  עבור הכפתור השלישי שבוטל. לא מזיקה. מועמדת לניקוי.

- **קוד מת של הכפתור השלישי** ב-`users_viewer_tzvira_v2.html`: ענף
  `if (false)`, הבורר `.btn-raw-summary` בשורה 2059, והמשתנה `canRaw`.
  נוטרל ולא נמחק, לפי "תוספת לפני מחיקה".

- **מטופל עם יותר מ-3 שיחות ביום** — הרביעית ואילך אינן בהקשר עד שהמהלך
  היומי ירוץ. **הפער היה קיים לפני** `ab` **ונסגר ב-16.9**, כשהחיתוך
  לשלושה הוסר מ-`get_summarized_linked_talk_v2`. נשאר רק גבול התקרה.

- **שני קובצי מיגרציה חסרים במאגר:** `intake_decision_add_gender_20260917`
  אין לו קובץ; `20260917_dev_delete_person_by_phone.sql` — לא ידוע אם נשמר.

- **שליחת מיילי התראה אינה נרשמת** ב-`events_intake`.

- **`dev_delete_person_by_phone` — למחוק לפני מעבר לייצור.** בדיקות בלבד.
  עוצרת ולא מוחקת דבר אם יש רשומות ב-`conversation_events_v2`, ואינה נוגעת
  בגיליון — ולכן הסנכרון הבא עלול להחזיר את האדם.

- **`bak_20260917_intake_decision` — לנקות בניקוי הבא.**

- **טלפונים גלויים למיסוך או ניקוי:** `corrector_test_log` · `otp_send_log` ·
  `users_total_postprocess_queue` · `app_guidance` · `users_total` ·
  `risk_reviews_v2` · `bak_20260814_risk_reviews_v2` · טבלאות
  `*_old_to_delete` וגיבויים.

- **אי-התאמה בנרמול המגדר:** `tools/intake_processor.py` מקבל רק `M` או `F`;
  פונקציית המסד מקבלת גם "זכר", "נקבה", "ז", "נ". הסקריפט מחמיר, ולכן
  הפרומפט מחייב אותיות לטיניות.

- **הקומיט `739831d` רשום בשם `Copilot`** — `.git/config` של המאגר הוגדר כך
  ותוקן ב-17.9. הקומיט נשאר כפי שהוא.

---

## 5. לקחים חדשים

**סימן מים ריק אינו "אין שליחות" אלא "אין גבול".** טבלה שנוצרה ריקה הייתה גורמת להרצה הראשונה לשלוח את כל ההיסטוריה. **מנגנון סימן מים מחייב זריעה מפורשת לפני התזמון הראשון.**

**היעדר ממצא במסד אינו ראיה להיעדר מנגנון.** מנגנון המייל הקיים לא נמצא בסקירת המסד משום שהוא חי ב־`GitHub Actions`, וכתובת הנמען יושבת בסוד ולא בקוד. **סודות אינם ניתנים לחיפוש.**

**שם קובץ גיבוי אינו עדות למה שהשתנה.** `before_pushover` הבטיח שינוי `Pushover` והכיל שורת סוד בלבד.

**ברירת מחדל של הרצה ידנית חייבת להיות הבטוחה.** `dry_run` מוגדר `"1"` בהרצה ידנית, `"0"` בהרצה מתוזמנת.

**הוראות ביצוע מופרדות מהסבר.** פקודה להרצה עומדת בבלוק נפרד, בלי לוגיקה משולבת.

**`git add` על קובץ בלי `git status` תחילה** גרר 1,512 שורות לא־מבוקרות למאגר ציבורי.

**תיעוד אינו מקום למזהים אמיתיים.**

**כל `fetch` יוצא מפונקציית קצה חייב מגבלת זמן.**

**`limit` בלי `order by` אינו חיתוך — הוא הגרלה.**

**פער של שלוש שעות בדיוק הוא חשד לאזור זמן, לא לעיכוב תפעולי.** (14.8.2026 — ההנחה השגויה שהיה עיכוב בעיבוד פתחה בירור מיותר לפני שהוצלב מול `now()`.)

**כלל שיש לאמץ: כל טבלה חדשה בסכימה `public` נולדת פתוחה** — כמו ייצור, בדיוק כך גיבוי. חזר על עצמו פעמיים: ב־14.8.2026 (חמש טבלאות) וב־13.9.2026 (`users_information_v2_daily_backup`).

**כל סריקה במאגר חייבת להיות עיוורת ל־`.gitignore`.** (14.8.2026 — סריקה ראשונה פספסה קובץ שלם מוחרג, ובו שמונה מופעים.)

**זיקוק אינו שלב אופציונלי בסוף הסשן — 13.9.2026.** קובץ זה המשיך להציג פריט שכבר נסגר (מסלול הקורס, 11.8) כ"המשימה הראשונה לסשן הבא" חודש שלם אחרי שהמסד השתנה. סשן שנפתח לפי התיעוד הזה היה מתחיל בתיקון של בעיה שאינה קיימת. היומן הגולמי היה מעודכן לאורך כל התקופה — הפער נפתח בשלב הזיקוק בלבד. **פריט שנסגר ולא הוסר מהרשימה המזוקקת גרוע מפריט שלא נרשם מעולם.** (מתועד גם כלקח 22.18 ב־`PROJECT_GUIDE.md`, לעדכון עתידי באותו קובץ.)

### ניקוי מיותרים במסד — עתידי, לא הותחל

סדר מחייב: גיבוי מלא -> ורק אז מחיקה.
במסד אין היסטוריה. DROP או CREATE OR REPLACE משמידים
את העותק היחיד של גוף הפונקציה.

היקף מאומת 7.8.2026:
96 פונקציות בסכמה public, מהן 76 SECURITY DEFINER.
כ-110,656 תווים, כ-2,800 שורות SQL.
במאגר קיימות 4 בלבד, בשלושה קבצי SQL תחת
supabase/migrations שלא עודכנו מאז 18.5.2026.
92 פונקציות קיימות במסד ובו בלבד.

כלי זיהוי — מאומת מה עובד ומה לא:
- track_functions = none. פונקציות אינן נמדדות כלל,
  pg_stat_user_functions ריקה. אין דרך לדעת אילו
  פונקציות נקראו אי פעם.
- pg_stat_user_tables פעילה על 81 טבלאות, ו-stats_reset
  ריק. לכן טבלה עם אפס קריאות לא נקראה מעולם —
  ראיה חזקה, לא השערה משם הטבלה.
- תלויות במסד וחיפוש טקסטואלי בגופי כל הפונקציות
  תופסים קריאות פנימיות במדויק.

גבול קשיח: קוראים בקנבס של Landbot, בפונקציות הקצה
וב-GitHub Actions אינם נראים במסד לעולם. היעדר קורא
במסד אינו ראיה לנטישה.

מועמדים ראשונים: תשע הטבלאות הישנות, PROJECT_GUIDE
סעיף 4.6. **עודכן 13.9.2026:** להוסיף למועמדות גם את
`users_information_v2_daily_backup` —
בדיקת חיוּת לפני כל החלטה, כרגיל. (המספור הישן שהפנה לכאן נמחק ב-13.9.2026; ראו העדכון בסעיף 1.)

---

## 8. מערכת Meitar — ההסבה מלנדבוט לקוד

**הבוט הטיפולי הוסב מלנדבוט לקוד.** בוט הקורס **טרם הוסב** — יוסב בהמשך,
ואז הלנדבוט יהיה מיותר לחלוטין.

**מקור השיחה — המפתח לקריאת הנתונים:**

`A`

— הלנדבוט. 430 הפעלות, האחרונה 31.8.2026.

`B`

— בוט הקורס, עדיין בלנדבוט. חמש הפעלות, האחרונה 19.8.2026.

`C`

— מערכת

`Meitar`.

42 הפעלות, מ-27.8.2026 ועד 2.9.2026.

**אומת בהצלבת תאריכים:** ב-2.9.2026, היום האחרון עם פעילות, רצו 14 הפעלות
בערך

`C`

ואפס בערך

`A`.

### חמש פונקציות קצה חדשות — אף אחת אינה מתועדת

כולן בגרסה 1, כלומר נפרסו פעם אחת ולא עודכנו מאז. **לכולן**

`verify_jwt = false`

**ו-**

`CORS`

**פתוח ב-**

`*`.

| פונקציה | נפרסה | תפקיד |
|---|---|---|
| `new-bot-n-1` | 23.8.2026 | דף עצמאי, פרומפט קבוע בקוד |
| `meitar-chat-proxy` | 26.8.2026 | מעביר בלבד אל `runtime-corrected-response` |
| `meitar-lookup-proxy` | 26.8.2026 | עוטף את `get_last_users_thread_v2` |
| `meitar-otp-gate` | 27.8.2026 | שער אימות, מרווח שעה |
| `sim-patient-proxy` | 1.9.2026 | **גופה לא נקרא** |

**אומת בקריאת קוד** לארבע הראשונות.

`meitar-chat-proxy`

מחזיק את

`LANDBOT_WEBHOOK_SECRET`

בצד השרת ואינו מכיל שום לוגיקה קלינית.

`meitar-lookup-proxy`

ו-

`meitar-otp-gate`

מחזיקים

`service_role`.

`new-bot-n-1`

**אינה נוגעת במסד כלל** — הפרומפט מוטמע בגופה, ויש לה רשומה מקבילה בטבלת
הפרומפטים בשם

`new_bot_therapist_N_1`.

**מעמדה מול מערכת**

`Meitar`

**לא הוברר.**

### חוסם ייצור — כל קודי האימות לכתובת אחת

ב-

`meitar-otp-gate`

הקבוע

`EMAIL_FOR_OTP_INTERIM`

מכיל כתובת דוא"ל יחידה, קבועה בקוד. **כל קוד אימות, לכל טלפון, נשלח אליה.**
מסומן בקוד כזמני. **מטופל אינו יכול לקבל את הקוד שלו.**

טבלה חדשה:

`otp_send_log`

— שורה אחת, סגורה כראוי.

### הערת קוד שקרית

ב-

`meitar-lookup-proxy`

כתוב שהרשאת ההרצה של

`anon`

על

`get_last_users_thread_v2`

נשללה. **היא לא נשללה — אומת במסד 13.9.2026.**

הפונקציה

`SECURITY DEFINER`,

**מפענחת**

`name_enc`,

ומחזירה שם, טלפון ו-

`user_text`

לפי מספר טלפון. פתוחה ל-

`anon`

— כלומר למפתח המוטמע בכל דף

`HTML`

במאגר. **חשיפה פעילה.**

**נסגר אגב כך:** הפריט הישן שדרש לתקן את הפונקציה לפענוח

`name_enc`

— בוצע. החוסם למחיקת העמודות הגלויות הוסר.

### מקור בגיט — נסגר 13.9.2026

`new-bot-n-1`

הייתה קיימת במאגר מלכתחילה, תחת

`New_bot/supabase/functions/`

ארבע האחרות לא היו קיימות באף קומיט מ-566 הקומיטים בכל הענפים. **הבדיקה
כללה בקרה** על

`runtime-corrected-response`

שהחזירה תוצאה, ולכן האפסים לא היו ארטיפקט של חיפוש שבור. כולן גובו —
ראו פריט 20.

**לקוח מיתר:**

`meitar_v2.html`

בשורש המאגר, מקומט, עודכן 28.8.2026. הקובץ היחיד במאגר הקורא לפונקציות
מיתר.

`new_bot_N_1.html`

**אינו קיים במאגר**, אף שהערת הקוד ב-

`new-bot-n-1`

מפנה אליו.

### שני ממצאים מקריאת הקוד הישן — לא טופלו

`secure-users-information`

כותבת לטבלה הישנה

`users_information`

בלי סיומת, ומפתח ההצפנה שלה **נופל לערך ברירת מחדל הכתוב בקוד** אם משתנה
הסביבה חסר. ייתכן שהיא מצפינה במפתח ידוע. **לא אומת אם היא נקראת כלל.**

`crypto-tool`

כלי הצפנה כללי, מוגן בכותרת סוד, **אינו נוגע במסד**. בשגיאת הרשאה מחזיר
אורכים בלבד.

---

## 9. מנוע התשובה — מצב מאומת 13.9.2026

**נקרא הקוד הפרוס, גרסה 58. פירוט מלא ב-**

`docs/SESSION_LOG.md`

**ברשומת 13.9.2026.**

### החוזה

שלושה שדות חובה בלבד:

`question20`

`patient_id`

`session_id`

**כל ההקשר נאסף בשרת** — סיכום, היסטוריה, פרומפטים, ורקע אישי.
הלקוח אינו יכול לזייף הקשר קליני ואינו יכול להשמיטו.

### ניתוב לפי מסלול

`CLINIC`

ו-

`NLP_CBT`

ממופים. ברירת המחדל היא

`NLP_CBT`.

### מנגנון הבקרה הקליני

מהלך מחייב לכל תור, ארבעה גלאי כשל, וחמש טבלאות יומן. סגורות כראוי.
751 מהלכים ב-81 שיחות.

### נתוני הפעלה

2,937 תורים מ-1.8 עד 2.9.2026. **24% נכתבו מחדש על ידי המתקן.**

---

## 10. מסלול הקורס — מצב מאומת 15.9.2026

**פירוט מלא ב-**

`docs/SESSION_LOG.md`

**ברשומת 15.9.2026.**

### עקרון ההכרעה

**מצב הקורס נקבע לפי השיחה, לא לפי המטופל.** חמישה מטופלים נושאים
`status = 3` והם גם מטופלים וגם קורסיסטים; מסלול הטיפול שלהם אינו יודע
לאיזה בוט נכנסו עכשיו.

הסימן הוא קיום שיעור מקושר לשיחה, **ושיעור יכול להתקשר רק לשיחה במקור**

`D`

— נאכף במסד ב-

`set_course_lesson_v2`,

לא בקוד. **מפת הניתוב לפי מסלול אינה נגעת כלל.**

### רכיבים

טבלה:

`conversation_course_lesson_v2`

פונקציות:

`set_course_lesson_v2`

·

`get_course_material_v2`

·

`get_course_lessons_v2`

מקור השיחה:

`D`.

המגבלה הורחבה ל-

`A,B,C,D`.

### התנהגות במנוע

אין מתקן · המנגנון הקליני חסום גם למטופל קליני · תקציב פלט 1,500 מול
500 · החומר מוזרק כהקשר.

### תוכן

30 שיעורים, 60 דפים. הדף המרוכז מכיל את הטקסט המלא של השיעור האחרון
ואת המקוצרים של הקודמים. הבנייה בסקריפט הגיליון.

## 11. טבלת הטקסטים

`app_texts_v2`

— 12 מפתחות. גיליון עצמאי

`BOT_TEXTS`

עם תפריט משלו. הטבלה סגורה; הדף קורא דרך

`get_app_texts_v2()`.

**ארבעה מחוברים לדף מית"ר**, כולם עם נפילה אחורה לנוסח הקיים.

**שלושה ריקים:**

`blocked_inactive`

·

`blocked_wrong_gate`

·

`otp_sent`

**כלל:** שינוי שם מפתח בגיליון יוצר שורה חדשה ואינו מוחק את הישנה.

---

## 12. מסלול ab — מצב מאומת 16.9.2026

**פירוט מלא ב-**

`docs/SESSION_LOG.md`

**ברשומת 16.9.2026.**

### העיקרון

`users_total_v2.ab`

מחזיק תמונת מצב מכווצת של העבר הרחוק, עד 600 תווים. לצידה נשארים שלושת
הסיכומים האחרונים. **המודל מקבל את שניהם, וכך גם המסך.**

**מדוע לא** `ab` **בלבד:** שיחה שהסתיימה אחרי הריצה האחרונה אינה בו.

### רכיבים

עמודות:

`ab`

·

`ab_updated_at`

פונקציות:

`ab_pending_v2`

·

`ab_apply_v2`

·

`patient_ab_for_viewer`

פרומפט:

`ab_summary`

— ברשימת המשותפים ב-`get_prompt_v2`

קבצים:

`tools/ab_processor.py`

·

`.github/workflows/ab-processor.yml`

— יומי ב-04:10 UTC

### הגנות

הקיצוץ נגזר במסד מהשדה ברגע הכתיבה, ולא ממה שהסקריפט קרא ·
`ab` ריק נדחה בשגיאה · תקרת ביטחון של 8,000 תווים ב-
`get_summarized_linked_talk_v2` · מייל לאדמין בכל ריצה שבה היה כשל

### המסך

**הצג מקוצרת** — הסיכום של השיחה הזו, מטבלת הצבירה.

**תמונת מצב** — `ab` ועוד הסיכומים שטרם נאספו.

**שום סיכום אינו נופל ביניהם:** או שהוא ברשימת האחרונים, או שנטמע ב-`ab`.

---

## 13. פענוח נתוני מטופל למנהל — 17.9.2026

**פירוט מלא ב-**

`docs/SESSION_LOG.md`

**ברשומת 17.9.2026.**

### 13.1 רכיבים

`admin_patient_lookup_v2(uuid)`

— בדיקת מנהל, תקרה של עשרים ניסיונות בשעה. **כל ניסיון נספר, גם כושל.**

`admin_patient_lookup_by_masked_phone_v2(text)`

— **בדיקת המנהל ראשונה**, ולכן מי שאינו מנהל אינו לומד אם הטלפון קיים.
מחזירה את כל ההתאמות, כי טלפון ממוסך אינו ייחודי.

`admin_patient_lookup_log_v2`

— הוספה בלבד, עדכון ומחיקה חסומים בטריגרים.

**הרשאות:**

`authenticated`

בלבד.

`anon`

חסום. הפעלה מ-

`SQL Editor`

מחזירה תמיד

`forbidden`.

**סירוב מוחזר כתשובה רגילה ולא כשגיאה**, כדי שרשומת היומן תישמר.

**תלויות שאסור למחוק בניקוי:**

`intake_dec`

·

`current_user_is_admin`

### 13.2 כלי מקומי

`C:\Users\User\Documents\meitar-local-tools\patient_lookup\`

**מחוץ למאגר.** אסימון בזיכרון בלבד, התנתקות בסיום, שום סוד אינו נשמר.

**להעביר רק**

`patient_code`.

לעמודה

`id`

יש אותה צורה אך היא מזהה שורה, והתשובה תהיה

`not_found`.

**נדחה:** פונקציית כניסה נפרדת למנהל. הכלי משתמש ב-

`start-login`

המשותפת, ולכן טלפון של כל מטפל רשום יקבל קוד. **מה שמגן:** מטפל שאינו
מנהל מקבל

`forbidden`,

והניסיון נרשם.

### 13.3 תיקונים לתיעוד — אומתו 17.9.2026

`get_last_users_thread_v2`

— **אין הרשאת**

`anon`.

`otp_send_log`

— סגורה, **אך שומרת טלפונים מלאים וגלויים.** ארבע שורות.

`corrector_test_log`

— **כ-2,800 טלפונים גלויים, יותר מכל טבלה אחרת.**

`patient_identity_map.phone`

— **אינו מקור אמין לטלפון ממוסך.** 28 מתוך 34 השורות ריקות.

**הטלפון הממוסך ב-**

`users_information_v2`

— שלוש ספרות, שלוש כוכביות, שלוש ספרות. כל ערך ייחודי היום. **אינו מתחיל
בהכרח ב-0.**

---

## מסלול הקבלה — מצב פתוח

**עובד מקצה לקצה ואומת חי:** שיחה, איסוף, הכרעה, יצירת מטופל, התראות.

**פתוח:**
1. הקישור לערוץ הטיפולי — intake_patient_link ריק, ולכן הודעת הקבלה נשלחת
   בלי כתובת להיכנס אליה.
2. קובצי ההגירה של המסלול אינם מלאים. הטבלאות והפונקציות הראשונות תועדו
   ב-migrations\intake, אך כל מה שנוסף אחריהן קיים במסד בלבד.
3. מסך המעקב מוגן באסימון בלבד, ומיועד לשלב הבדיקות.
4. ארבעה מטופלי בדיקה מסומנים patient_origin = INTAKE. למחוק לפני ייצור.
5. משמעות ערכי status אינה מתועדת. נקבע 1 למסלול לפי הכרעה, לא לפי ידיעה.
6. הפרומפטים לא נסקרו מקצועית.
7. אין זיהוי סיכון בזמן אמת בשיחת ההיכרות, במכוון. רק אמירה מפורשת עוצרת
   את האיסוף ומפנה לקווי סיוע, ומסומנת לאדמין.
