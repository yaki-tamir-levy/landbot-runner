# ארכיטקטורת ההצפנה והזהויות — BOT V2

**גרסה 3.0 — 30 ביולי 2026**

**מעמד:** מחליף את גרסה 2.0. שינוי מהותי: מסלול הפענוח **חובר ופועל**. גרסה 2.0 קבעה שהוא נבנה ונעקף — הקביעה בוטלה.

---

> ## אזהרת תאריך — נוסף 20.9.2026
>
> **גוף המסמך נכתב ב-30–31.7.2026 ולא נבדק מאז.** הוא נשאר כשינוי לא
> מקומט בעץ העבודה במשך כשבעה שבועות, ונדחף לראשונה ב-20.9.2026 כדי
> שהמאגר יחזיק את הגרסה המלאה במקום את גרסת 27.7 שקדמה לה.
>
> **הדחיפה אינה אימות מחדש.** הפרטים כאן מדויקים ליולי, ולא לספטמבר.
>
> **שלושה שינויים מאומתים שקרו אחרי כתיבתו, ואינם משוקפים כאן:**
>
> `decrypt_user_text`
>
> ו-
>
> `get_last_users_thread_v2`
>
> **נסגרו שתיהן.** הרשאות ההרצה שלהן הן
>
> `postgres`
>
> ו-
>
> `service_role`
>
> בלבד — אומת ב-
>
> `pg_proc`
>
> ב-19.9.2026. **כל תיאור כאן של חשיפה פתוחה ל-**
>
> `anon`
>
> **בשתי הפונקציות האלה — מבוטל.**
>
> `get_last_users_thread_v2`
>
> **מפענחת כיום**
>
> `name_enc`.
>
> החוסם למחיקת העמודות הגלויות מטבלת הזהות **הוסר** ב-13.9.2026.
>
> **ספירות השורות השתנו בסדר גודל.**
>
> `patient_identity_map`
>
> מונה 34 שורות נכון ל-20.9.2026, לא שש. ראו סעיף 4.2 ב-
>
> `PROJECT_GUIDE.md`.
>
> **מקור האמת העדכני הוא**
>
> `PROJECT_GUIDE.md`
>
> **(מאומת חלקית 20.9.2026) ו-**
>
> `docs/SESSION_HANDOFF.md`
>
> **(20.9.2026).** מסמך זה הוא פירוט משלים ליולי, ובכל סתירה — הם גוברים.
>
> **פריט פתוח:** אימות מחדש של המסמך כולו מול המסד, סעיף-סעיף.

---

**מסמך משלים ל־** `PROJECT_GUIDE.md`. המדריך מכיל תמצית; כאן הפירוט המלא.

---

## 0. כללי קריאה

**מאומת** = נבדק בשאילתה ישירה מול המסד או בקריאת קוד, בתאריך שלמעלה.

**לא אומת מחדש** = הופיע בגרסה קודמת וטרם נבדק שוב.

---

## 1. מטרת השכבה

הפרדה בין זהות המטופל, נתוני התפעול, נתוני השיחות, והנתונים המוצגים.

המטרה: לצמצם ככל האפשר שימוש במספר הטלפון המלא ובשם המטופל לאורך המערכת.

**מצב המטרה נכון להיום:** מושגת בשכבת הזהות. השם והטלפון נכתבים מוצפנים ונקראים מפוענחים בצד השרת בלבד. **טרם מושגת** בשלוש נקודות שנותרו — ראו סעיף 14.

---

## 2. מודל הזהות — ארבעה ייצוגים

### 2.1 patient_code

מזהה

`uuid`

פנימי. ברירת מחדל:

`gen_random_uuid()`

תפקידו: קישור בין כל הטבלאות. אינו מכיל מידע אישי.

### 2.2 phone_hash

טלפון ← ניקוי רווחים ← הסרת כל תו שאינו ספרה ←

`SHA-256`

←

`Hex`

משמש לאיתור מטופל. אינו מאפשר שחזור.

מוגדר

`UNIQUE`

ולכן לא ייתכנו שני מטופלים עם אותו ערך.

### 2.3 phone_enc

הטלפון המלא, מוצפן. **אומת: מאוכלס בכל השורות ללא יוצא מן הכלל.**

### 2.4 phone — עמודת צל

טלפון ממוסך. לדוגמה:

`0544973798`

הופך ל־

`054***798`

**מאומת:** טלפון באורך שש ספרות או פחות נשמר **גולמי, ללא מיסוך**. בנתוני הבדיקה נמצאו ערכים כמו

`123`

,

`987`

,

`1234`

**נקודת כשל שהתממשה בפועל:** השוואה של

`phone`

למספר מלא מחזירה אפס שורות **בשקט**. הפונקציה

`set_disclaimed_agreed`

עושה בדיוק זאת, ולכן אינה מעדכנת דבר — ראו סעיף 14.5.

---

## 3. הטבלאות

### patient_identity_map — שכבת הזהות

**מאומת (30.7.2026): שש שורות.** לפני הניקיון היו 39.

עשר עמודות:

`patient_code`

`phone_hash`

`phone_enc`

`name_enc`

`email_enc`

`created_at`

`updated_at`

`phone`

`name`

`email`

**מוצפן:** שלוש העמודות בסיומת

`_enc`

**גלוי:** שלוש עמודות הצל.

**חשוב:** ההצפנה נוספה **בתוספת** לעמודות המקוריות ולא במקומן. שלב המחיקה טרם בוצע. זהו הפער המרכזי שנותר — ראו סעיף 14.1.

### users_information_v2 — שכבת התפעול

**מאומת: אין בה עמודת** `name`. אחת־עשרה עמודות:

`timestampz`

`patient_code`

`phone`

`user_text`

`active`

`conversation`

`created_at`

`updated_at`

`status`

`disclaimed`

`psychologist`

אינה מחזיקה טלפון מלא. **מאומת: ארבע שורות** אחרי הניקיון.

**לא אומת:** תפקיד

`timestampz`

מול

`created_at`

### app_config — מפתח ההצפנה

השורה הרלוונטית נושאת את המפתח:

`crypto_key_b64`

**מאומת:** מדיניות אחת חוסמת גישה ישירה.

---

## 4. מנגנון ההצפנה

`pgp_sym_encrypt()`

עם

`cipher-algo=aes256`

ו־

`compress-algo=0`

כלומר:

`OpenPGP`

,

`AES-256`

, ללא דחיסה.

הפענוח:

`extensions.pgp_sym_decrypt()`

על הערך לאחר הסרת הקידומת ופענוח

`Base64`

---

## 5. פורמט האחסון

בינארי ←

`Base64`

← הוספת הקידומת

`db1:`

← שמירה

התוצאה:

`db1:<Base64>`

**מאומת:** כל השורות נושאות את הקידומת בעמודות המוצפנות.

---

## 6. מפתח ההצפנה

נשלף מ־

`public.app_config`

לפי

`crypto_key_b64`

בהיעדר המפתח, הפונקציות עוצרות ב־

`Exception`

**שלוש פונקציות ניגשות למפתח:**

`get_or_create_patient_code`

`upsert_users_information_v2_from_sheet`

— מאז 30.7.2026

`rpc_get_users_tzvira_v2_viewer`

---

## 7. יצירת מטופל חדש

טלפון ← נרמול ←

`SHA-256`

←

`phone_hash`

← חיפוש

**אם נמצא:** מוחזר

`patient_code`

קיים. **הפונקציה יוצאת מיד ואינה נוגעת בהצפנה.**

**אם לא נמצא:** נקרא המפתח, מוצפן הטלפון, מוצפן השם, מבוצע

`INSERT`

, ונוצר

`patient_code`

חדש.

**מאומת: חתימה אחת בלבד.** אין סיכון העמסה בפונקציה זו.

**אומת גם:** אין בה שום סינון של הקלט. טלפון כמו

`99999999`

תקין מבחינתה לחלוטין, וייצור זהות. זהו המנגנון שיצר בעבר שש זהויות עבור שורות פרומפט.

---

## 8. עדכון מטופל מגיליון — שונה 30.7.2026

טלפון ← נרמול ← מיסוך ←

`get_or_create_patient_code()`

← **הצפנה וכתיבה ל־**

`patient_identity_map`

←

`UPSERT users_information_v2`

**מבוטל:** האזהרה על שתי חתימות. **החתימה בת ששת הפרמטרים נמחקה** לפי חתימה מלאה. נותרה חתימה אחת בת שבעה פרמטרים.

**מבוטל:** החשש שקריאה בשישה פרמטרים תגיע לגרסה הישנה. אומת ש־

`supa-sync`

שולחת תמיד את כל שבעת הפרמטרים בשמם, כולל

`p_psychologist`

, גם כשערכו ריק.

### מה השתנה בגוף הפונקציה

**קודם:** הפונקציה כתבה

`name`

,

`phone`

ו־

`email`

לעמודות **הגלויות**.

**כעת:** היא כותבת ל־

`name_enc`

ול־

`email_enc`

בלבד, בהצפנה זהה לזו של

`get_or_create_patient_code`

`phone_enc`

**אינו נכתב** — הטלפון הוא מפתח החיפוש, ושינוי שלו יוצר מטופל חדש.

**תוקן באותה הזדמנות:** ערך ריק **אינו דורס** עוד ערך קיים. קודם לכן ההשמה נעשתה ללא תנאי, ולכן תא שם ריק בגיליון מחק את השם השמור.

**אומת חי:** שינוי שם בגיליון מתפשט לאורך כל השרשרת ומופיע ב־

`Viewer`

### התנהגות תאים ריקים — הבדל בין שתי החוליות

ה־

`Apps Script`

**משמיט** מפתח שערכו ריק.

הפונקציה

`supa-sync`

**שולחת**

`null`

מפורש, דרך

`getOptionalText`

זהו ההבדל שהסביר את השורות שנמצאו עם

`name_enc`

וללא

`name`

---

## 9. מה מוצפן ומה לא — עודכן

| שדה | מוצפן | הערה |
|---|---|---|
| טלפון | כן | נכתב ל־`phone_enc` ביצירה בלבד |
| שם | כן | **ביצירה וגם בעדכון מגיליון** |
| מייל | כן | נכתב ל־`email_enc` בעדכון מגיליון |

**מבוטל:** הקביעה שהמייל אינו מוצפן. **מבוטל:** הקביעה שהשם מוצפן ביצירה בלבד.

---

## 10. מה נשמר גלוי

בטבלת הזהות עדיין קיימות שלוש עמודות גלויות:

`name`

`phone`

— ממוסך

`email`

**אין להן כותב.** הכתיבה היחידה אליהן הוסרה ב־30.7.2026.

**יש להן שני קוראים במסד**, ולכן טרם נמחקו — ראו סעיף 14.2.

---

## 11. מסלול Landbot

טלפון ← נרמול ←

`SHA-256`

←

`patient_identity_map`

←

`patient_code`

←

`users_information_v2`

`Landbot`

**אינו מפענח נתונים.**

**לא אומת מי הקורא בפועל.** הפונקציה

`get_last_users_thread_v2`

מחזירה בדיוק את מבנה הנתונים שנדרש בפתיחת שיחה, אך **אין לה ולו קורא אחד במאגר**. ייתכן שהקריאה נעשית מהקנבס של

`Landbot`

, ואז אין לה עקבה בקוד.

---

## 12. מסלול Viewer — חובר (30.7.2026)

### מה כתבה גרסה 2.0

שה־

`Viewer`

קורא את עמודות הצל הגלויות ואינו מפענח, ושהפונקציה המאובטחת נבנתה ונעקפה.

**הקביעה הייתה נכונה לזמנה, ובוטלה.**

### המצב כעת

בשורה 590 ה־

`Viewer`

קורא ל־

`rpc_get_users_tzvira_v2_viewer`

הפונקציה

`SECURITY DEFINER`

, מפענחת את

`name_enc`

ואת

`phone_enc`

בצד השרת, ומסננת לפי אותו תנאי בדיוק שבמדיניות ה־

`RLS`

**טקסט גלוי אינו עובר עוד בחוט מטבלת הזהות.**

הקומיט:

`f1aac5f`

### החשיפה שנמצאה בדרך — קריטית

לפני התיקון, הפונקציה הייתה:

`SECURITY DEFINER`

**ללא שום תנאי סינון**, ועם הרשאת הרצה ל־

`PUBLIC`

ול־

`anon`

**המשמעות:** קריאה אחת ל־

`/rest/v1/rpc/rpc_get_users_tzvira_v2_viewer`

עם המפתח הגלוי בקוד הדף הייתה מחזירה **את כל השמות והטלפונים מפוענחים, ללא התחברות כלל.**

היא עקפה גם את שער האימות בשורה 302 וגם את שמונה מדיניות ה־

`RLS`

**סדר התיקון:** שלילת ההרשאה ← הוספת הסינון ← החזרת ההרשאה ל־

`authenticated`

בלבד.

### מה שנשמר בשינוי

חוזה המוכנות של

`CACHE.loaded`

וטיפול הכשל הרך. כשל בפענוח משאיר את הממשק שמיש, ורק החיפוש לפי שם מושבת.

### שינוי בתצוגה שהוחלט עליו במפורש

העמודה הגלויה החזיקה טלפון **ממוסך**. הפונקציה מחזירה טלפון **מלא**.

**הוחלט להשאיר מלא:** רק הפסיכולוג המטפל רואה את המסך, הגישה מסוננת בשתי שכבות, והוא זקוק למספר האמיתי כדי ליצור קשר.

### מה שנותר גלוי ב־Viewer

שורה 559 שולפת

`name`

ו־

`phone`

מ־

`risk_reviews_v2`

— **נשלחות לדפדפן ואינן מוצגות.** וכן שורה 987 במסלול הנסיגה.

**אומת שהצינור סביב**

`data-name`

**ו־**

`data-phone`

**הוא קוד מת** — הן ניזונות מטבלה שאין בה עמודות אלה, וערכן

`undefined`

תמיד.

---

## 13. הרשאות ו־RLS

### 13.1 המדיניות — מאומת

שמונה מדיניות על חמש טבלאות, כולן על התפקיד

`authenticated`

**אין ולו מדיניות אחת עבור**

`anon`

**באף אחת מהן.**

| טבלה | שליפה | עדכון | הוספה |
|---|---|---|---|
| `patient_identity_map` | יש | — | — |
| `users_information_v2` | יש | — | — |
| `users_tzvira_v2` | יש | — | — |
| `risk_reviews_v2` | יש | יש | — |
| `talk_read_flags` | יש | יש | יש |

> **התגלה 31.7.2026 ערב — טבלה שאינה ברשימה.**
>
> `app_guidance`
>
> מחזיקה 21 שורות עם **טלפון ושם מלאים**. קיימות בה עמודות
>
> `phone_hash`
>
> ו־
>
> `email_enc`
>
> **ושתיהן ריקות לחלוטין** — כלומר ההצפנה הוגדרה כאן ומעולם לא הופעלה.
>
> הטבלה אינה מופיעה באף מסמך פרויקט. **יש לסווג אותה לפני כל החלטה.**

שרשרת ההיתר בכולן:

`current_user_is_admin()`

**או** קיום שורה ב־

`users_information_v2`

שבה

`psychologist`

שווה ל־

`current_psychologist_phone()`

**מדיניות העדכון נושאות את התנאי בשני השדות** —

`qual`

וגם

`with_check`

— ולכן שורה אינה יכולה לצאת מתחום הקורא. זו הצורה הנכונה.

**אין מדיניות**

`DELETE`

**על אף אחת מחמש הטבלאות.** מחיקה דרך

`PostgREST`

חסומה לחלוטין. סביר שזה מכוון, ואינו מתועד.

### 13.2 אי־עקביות בניסוח — אינה חור

התנאי

`psychologist IS NOT NULL`

מופיע בשתי מדיניות וחסר בשתיים אחרות. הוא **מיותר**: השוואה ל־

`NULL`

מחזירה

`NULL`

ולא אמת, ולכן מטופל ללא פסיכולוג אינו נראה בשום מסלול. ראוי לאחד לשם קריאות בלבד.

### 13.3 הרשאות EXECUTE — מאומת ומסוכן להסקה

**מאומת:** כמעט כל פונקציה בסכמה

`public`

מעניקה הרצה ל־

`anon`

ול־

`PUBLIC`

זו **ברירת המחדל של**

`PostgreSQL`

: פונקציה חדשה מקבלת

`EXECUTE`

ל־

`PUBLIC`

אוטומטית.

**שתי מסקנות מעשיות:**

אין להסיק מהרשאה שפונקציה נועדה לשימוש חיצוני, ואין להסיק שמישהו פתח אותה בכוונה.

**כל פונקציה**

`SECURITY DEFINER`

**שאינה מסננת בגופה היא חשיפה בפועל**, גם בלי שאיש נגע בהרשאות.

**היוצאת מן הכלל היחידה כיום:**

`rpc_get_users_tzvira_v2_viewer`

, שממנה נשללה ההרשאה במפורש.

### 13.4 שלוש פונקציות נגישות ל־anon — נסקרו, אחת נסגרה (31.7.2026 ערב)

`get_all_phones_and_talks`

— **נסגרה.** הייתה

`SECURITY DEFINER`

בלי שום סינון בגוף, והחזירה את כל

`users_total`

— טלפון ו־

`linked_talk`

— לכל מי שמחזיק את מפתח ה־

`anon`

גופה גובה במלואו, שמה שונה, ואז בוטלה הרשאת ההרצה. ה־

`ACL`

כולל כעת

`postgres`

ו־

`service_role`

בלבד. פירוט מלא בסעיף 15.7 של המדריך.

**הלקח שנלמד כאן חל על שתי האחרות:** שינוי שם לבדו לא הספיק, כי ההרשאה נסעה לשם
החדש. **הרצף הוא שלושה שלבים: גיבוי גוף, שינוי שם, ואז**

`revoke`

`get_all_phones`

— **פתוחה.** אינה

`SECURITY DEFINER`

ולכן כפופה ל־

`RLS`

של הקורא. **אך נושאת הענקה ל־**

`PUBLIC`

הרישום

`=X/postgres`

מופיע בראש רשימת ההרשאות שלה, וזו הענקה רחבה יותר מהענקה מפורשת ל־

`anon`

מאחר שאינה

`DEFINER`

היא כנראה אינה מחזירה נתונים — אך "כנראה" אינו אימות.

`decrypt_user_text`

— **פתוחה.** עדיין

`SECURITY DEFINER`

ועדיין מוענקת ל־

`anon`

הטבלה שלה שונתה ולכן היא שבורה כרגע. **שבור אינו סגור.**

---

## 14. פערים מאומתים

### 14.1 שני עמודים מקבילים — הפער המרכזי

טבלת הזהות מחזיקה שם, טלפון ומייל **פעמיים**: מוצפן וגלוי. ההצפנה נוספה ומעולם לא הוחלפה.

**מצב היום:** לעמודות הגלויות **אין כותב**. נותרו שני קוראים במסד.

### 14.2 שני חוסמים למחיקת העמודות הגלויות

`get_last_users_thread_v2`

מצטרפת לפי

`phone_hash`

, אך שולפת

`pim.name`

ו־

`coalesce(ui.phone, pim.phone)`

**התיקון אינו דורש לזהות את הקורא:** די להפוך אותה לפענוח

`name_enc`

, כפי שנעשה ב־

`set_disclaimed_agreed_v2`

מול הגרסה השבורה.

`conversation_events_v2_view`

מכילה

`pim.phone AS phone_masked`

, וממנה ניזון מסך המוניטור. מחיקת הטלפון תשבור אותו.

**שלוש פונקציות אינן חוסמות** — מצטרפות לפי

`phone_hash`

בלבד:

`get_summarized_linked_talk_v2`

,

`set_disclaimed_agreed_v2`

,

`trace_patient_flow_v2`

**העמודה**

`email`

**פנויה למחיקה.**

### 14.3 אין מסלול לשינוי טלפון

לא קיים מסלול לעדכון

`phone_hash`

ו־

`phone_enc`

למטופל קיים. שינוי טלפון ייצור

`patient_code`

חדש, ובכך יאבד את היסטוריית המטופל.

**נצפה בפועל בסשן זה:** הוספה מחדש מהגיליון מחזירה שם וטלפון, אך יוצרת מזהה חדש. השיחות, שורות ה־

`tzvira`

, ממצאי הסיכון והיומן קשורים למזהה הישן ואינם חוזרים.

### 14.4 שתי עמודות המייל ריקות

השורה היחידה שנשאה מייל נמחקה בניקיון. שתי העמודות ריקות לחלוטין ומועמדות למחיקה מבנית.

### 14.5 set_disclaimed_agreed שבורה בשקט

הפונקציה משווה טלפון מלא מול העמודה הממוסכת:

`058***798`

לאחר הסרת תווים שאינם ספרות נותן שש ספרות, מול עשר. **ההשוואה לעולם אינה מתקיימת.** הפונקציה רצה, אינה נכשלת, ואינה מעדכנת אף שורה.

הגרסה

`set_disclaimed_agreed_v2`

תקינה ומשתמשת ב־

`phone_hash`

**להכריע:** למחוק את הישנה, או לתקנה.

### 14.6 patient_identity_map_decrypted — נמחק (27.7.2026)

ה־

`View`

**נמחק.** לפני המחיקה: העניק ל־

`anon`

את כל ההרשאות, ביצע

`CROSS JOIN`

אל

`app_config`

ושלף את מפתח ההצפנה.

**ההגנה היחידה הייתה תקלה:** הפענוח נכשל משום שהקידומת

`db1:`

מכילה נקודתיים, פסול ב־

`Base64`

**הלקח שחזר בסשן הבא:** ההגנה הייתה תקלה, לא מנגנון. אותו דפוס בדיוק נמצא שוב ב־

`rpc_get_users_tzvira_v2_viewer`

— רק ששם לא הייתה אפילו תקלה שתגן.

---

### 14.7 מפת החשיפה המלאה — 31.7.2026 ערב

בוצעה סריקת סכמה מלאה של כל עמודות הזהות. **הרשימה המחייבת נמצאת בסעיף 29 של**

`SESSION_HANDOFF.md`

ואין לשכפל אותה כאן. שלוש נקודות מבניות ששייכות למסמך הזה:

**א. הטבלה היחידה שבה ההצפנה הושלמה.**

`patient_identity_map`

כל שש השורות מחזיקות במקביל

`phone_enc`

,

`name_enc`

,

`email_enc`

ו־

`phone_hash`

מאוכלסים. **זו הטבלה היחידה שבה מחיקת העמודות הגלויות אפשרית טכנית היום.** החוסמים
הם שני הקוראים בסעיף 14.2, לא מצב ההצפנה.

**ב. פער מבני, לא תפעולי.**

`users_information_v2`

מחזיקה

`phone`

כ־

`NOT NULL`

**ואין לה עמודה מוצפנת מקבילה כלל.** מחיקה שם דורשת קודם בנייה ומילוי של עמודה
חדשה. אותו מצב קיים ב־

`users_total_v2`

**ג. שתי תצוגות מחזירות טלפון ושם.**

`risk_reviews_view`

ו־

`users_total_api`

**תצוגה שנשענת על עמודה שנמחקה — נשברת.** יש לערוך אותן **לפני** מחיקת עמודה, לא
אחריה. לעומתן

`conversation_events_v2_view`

מחזירה

`phone_masked`

בלבד, וזה תקין.

---

## 15. תרשים — מעודכן

```
Google Sheet (BOT_PATIENTS)
     │
     ▼
Apps Script            ── משמיט תאים ריקים
     │
     ▼
supa-sync              ── שולח 7 פרמטרים תמיד, כולל null מפורש
     │
     ▼
upsert_users_information_v2_from_sheet   (חתימה אחת)
     │
     ├──► get_or_create_patient_code
     │         ├──► SHA-256 ──► phone_hash
     │         ├──► phone_enc      (ביצירה בלבד)
     │         └──► name_enc       (ביצירה בלבד)
     │
     └──► UPDATE patient_identity_map
               ├──► name_enc       (בכל עדכון, לא דורס בריק)
               └──► email_enc      (בכל עדכון, לא דורס בריק)
     │
     ▼
patient_identity_map
     │
     ├────────► Landbot   ← get_last_users_thread_v2   (עדיין גלוי — חוסם)
     │
     ├────────► Monitor   ← conversation_events_v2_view (עדיין גלוי — חוסם)
     │
     └────────► Viewer    ← rpc_get_users_tzvira_v2_viewer
                             SECURITY DEFINER + סינון + פענוח בצד שרת
                             EXECUTE ל־authenticated בלבד
```

---

## 16. מצב הרכיבים

| רכיב | מצב |
|---|---|
| `patient_code` | פעיל |
| `phone_hash` | פעיל, `UNIQUE` |
| `phone_enc` | פעיל — כתיבה ביצירה בלבד |
| `name_enc` | **פעיל — נכתב גם בעדכון** |
| `email_enc` | **פעיל — נכתב בעדכון.** ריק בפועל |
| `phone` גלוי | ללא כותב. שני קוראים חוסמים |
| `name` גלוי | ללא כותב. קורא אחד חוסם |
| `email` גלוי | ללא כותב, ללא קורא, ריק |
| `Landbot` | נתונים ממוסכים. הקורא לא אומת |
| `Viewer` | **מפענח בצד השרת** |
| מפתח ההצפנה | ב־`app_config` |
| `RLS` | 8 מדיניות על 5 טבלאות, `authenticated` בלבד |
| `rpc_get_users_tzvira_v2_viewer` | **מסונן, מוגבל, בשימוש** |
| `patient_identity_map_decrypted` | נמחק 27.7.2026 |

---

## 17. מסקנה

הארכיטקטורה מבוססת על שלושה עקרונות: זיהוי לפי

`phone_hash`

, קישור לפי

`patient_code`

, ושמירת מידע רגיש בהצפנת

`OpenPGP`

**גרסה 2.0 קבעה שההגנה נשענת על מיסוך ועל**

`RLS`

**— לא על ההצפנה. הקביעה בוטלה.**

מסלול הצפייה מפוענח בצד השרת, מסונן בגוף הפונקציה, ומוגבל ל־

`authenticated`

הכתיבה מהגיליון מוצפנת.

**מה שנותר פתוח:**

- שני קוראים במסד מונעים את מחיקת העמודות הגלויות
- שינוי טלפון עדיין שובר את זהות המטופל
- שלוש פונקציות נגישות ל־`anon` לא נסקרו
- נתיב עקיפה רדום ב־`supa-sync` שאינו עובר בהצפנה כלל

**ההגנה כיום נשענת על שלושה מנגנונים:** סינון

`RLS`

, סינון בגוף פונקציית הפענוח, וההצפנה עצמה. שלושתם פעילים.

---

## 18. מה לא אומת

- מערך התביעות המדויק באסימון גישה חי — הוסק ממסלול ההנפקה בלבד
- מבנה `risk_phrases`
- מי קורא ל־`get_last_users_thread_v2` — ייתכן שהקנבס של `Landbot`
- האם `.range()` חל על קריאת `rpc` בגרסת הספרייה שבשימוש
- גופן של `decrypt_user_text` ו־`get_all_phones`. **`get_all_phones_and_talks` נקרא במלואו 31.7 ערב** ומתועד ב־`SESSION_HANDOFF.md`
- האם קיימים מסלולי כתיבה נוספים ל־`name_enc` מחוץ לפונקציות שנבדקו
