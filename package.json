{
  "name": "landbot-runner",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node script_link.mjs",
    "install:pw": "npx playwright install chromium && npx playwright install-deps"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  }
}
